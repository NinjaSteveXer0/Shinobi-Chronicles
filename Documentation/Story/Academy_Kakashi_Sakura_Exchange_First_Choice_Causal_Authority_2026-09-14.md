# Shinobi Chronicles — Academy Kakashi Sakura Exchange First-Choice Causal Authority

**Date:** 2026-09-14  
**Owner:** Stephen / Writing / CE-Codex-Coordination  
**Status:** **STEPHEN-APPROVED STORY CAUSAL AUTHORITY — CONSUME THROUGH #187 STRUCTURED STORY REALISATION WORKFLOW**

## 1. Purpose

This document removes ambiguity around the first major player-choice family in Academy Kakashi's recovered Origin and records the intended causal structure so Stephen does not have to repeatedly reconstruct it from memory.

This authority is game-specific Story structure. It does not create new Battle, custody, stealth, Knowledge, or Chronicle Engine mechanics.

It must be consumed together with:

- `Documentation/Coordination/Structured Story Realisation Package Cross-Department Workflow 2026-09-14.md`;
- `Documentation/Story/Interactive_Scene_Performance_and_Branch_Realization_Doctrine_2026-09-14.md`;
- `Documentation/Story/CE_Intelligent_Story_Decision_Continuation_Requirement_2026-09-14.md`;
- `Documentation/Story/CE_Choice_Driven_Story_Direction_and_Trajectory_Requirement_2026-09-14.md`;
- the current recovered Academy Kakashi Origin authority.

## 2. Mission objective established by the ANBU

The operative Story objective given to Kakashi is:

> **Stop the package from falling into the wrong hands.**

This objective is the causal reason the later alley/Sakura-tree choice family matters.

The first choice family is not four flavour variants around one fixed paragraph. Each choice represents a materially different way Kakashi responds to the same live problem.

Current first-choice family:

- `ATTACK`;
- `PICKPOCKET`;
- `OBSERVE`;
- `GET CLOSER`.

## 3. ATTACK — direct intervention route

Stephen's intended Story structure is that choosing `ATTACK` is a direct attempt to fulfil the ANBU objective before the package cleanly reaches the wrong hands.

Binding branch-level Story result:

> **The clean package handoff into the wrong hands is prevented/interrupted on the ATTACK route.**

This does **not** mean Writing or presentation may infer every lower-level factual consequence.

Preserve:

- `ATTACK` != automatic Battle unless the legal resolver seam says Battle begins;
- `ATTACK` != automatic package ownership by Kakashi;
- interrupted transfer != every opponent defeated;
- objective secured at this stage != no later complication;
- player choice still does not command NPC reactions.

The exact method, resistance, Battle participation, position changes, injuries, escape, custody and downstream pressure remain owned by the relevant resolver/current Chronicle state unless separately authored.

Writing should therefore package `ATTACK` as a direct-intervention branch whose authored Story invariant is that the intended clean transfer is stopped, while CE/runtime/resolvers determine the factual manner and subsequent situation.

## 4. PICKPOCKET — stealth/interception route

Stephen's intended Story structure is that choosing `PICKPOCKET` is the stealth/interception route for accomplishing the same operative objective before the package cleanly reaches the wrong hands.

Binding branch-level Story result:

> **The package is prevented from completing the intended clean transfer into the wrong hands on the PICKPOCKET route.**

This does not author a generic `PICKPOCKET = success` rule for Chronicle Engine.

For this specific authored Kakashi Story branch, the high-level Story trajectory is fixed: Kakashi's intervention prevents the clean handoff. The owning runtime may still determine/represent the exact factual mechanism, detection state, custody state, resistance, cost, side effects and next pressure where those details are not separately fixed.

Preserve:

- `PICKPOCKET` != universal automatic theft success;
- prevented handoff != automatic permanent Kakashi custody;
- package interception != automatic Knowledge of the package's full meaning;
- stealth route != guaranteed undetected route unless separately resolved/authored.

## 5. OBSERVE — deliberate escalation / information route

Stephen's intended Story structure is explicit:

Choosing `OBSERVE` means Kakashi deliberately does **not** intervene before the handoff because he continues watching the exchange.

Binding authored sequence:

`OBSERVE`
→ Kakashi allows the exchange to continue
→ **the package is handed over**
→ **the decoy assassin / Rogue Chūnin escalation appears**
→ the situation becomes materially more complicated
→ a **new meaningful decision family** opens
→ at least one later branch from this escalation leads to **Pakkun's first involvement**.

This is not an unresolved CE invention point.

The package transfer and the decoy-assassin escalation belong to the authored `OBSERVE` continuation.

CE/runtime still owns/coordinates the exact current-state realisation around that authored structure, including actor-relative Knowledge, positioning, legitimate next intents, resolver dispatch, factual outcomes and persistence.

Important tradeoff:

- `OBSERVE` may preserve/gain an intelligence opportunity;
- but Kakashi pays for that information opportunity by allowing the package transfer to occur and the problem to escalate.

The later branch must consume the factual state created by that choice rather than pretending the transfer was still prevented.

## 6. Pakkun is authored later opportunity, not random CE invention

Pakkun's first involvement is part of Kakashi's Origin Story authority.

However:

> **Pakkun does not appear merely because CE decides a dog would be useful.**

The `OBSERVE` escalation opens further decisions. One later branch/path from that evolving state reaches the authored circumstance in which Pakkun becomes involved.

The precise later trigger/path should be recovered or deliberately approved as the structured package is completed.

Until that later trigger is fixed, preserve only these facts:

- Pakkun's first involvement belongs in the Origin;
- it arises downstream of the expanded post-`OBSERVE` problem space in at least one legitimate route;
- CE may not spawn Pakkun without the authored eligibility basis;
- Writing should flag the exact trigger/path as `RECOVERY / APPROVAL REQUIRED` rather than invent it silently.

## 7. GET CLOSER remains intentionally unresolved here

Stephen has not closed the exact causal branch contract for `GET CLOSER` in this clarification.

Therefore this document does **not** infer one.

Writing / CE should recover existing authority first. If the exact branch remains absent, return it to Stephen as a focused unresolved Story question rather than silently mapping it onto `OBSERVE`, `ATTACK`, or `PICKPOCKET`.

Canonical status:

> **GET CLOSER = meaningful approved intent; exact authored high-level continuation still requires recovered authority or deliberate approval.**

## 8. What Writing should deliver

Writing should now treat the Sakura-tree/alley decision as a Structured Story Realisation Package with at least:

- operative objective: stop the package reaching the wrong hands;
- exact pre-choice actors/positions/Knowledge/object custody;
- `ATTACK`, `PICKPOCKET`, `OBSERVE`, `GET CLOSER` semantic intents;
- the branch-level authored invariants above;
- decoy-assassin/Rogue Chūnin conditional/branch authority;
- Pakkun later-opportunity hook;
- important dialogue/character voice;
- resolver seams;
- prohibited inventions;
- exit/completion predicates;
- explicit unresolved items requiring recovery/Stephen approval.

Writing should **not** require Stephen to recover exact historical prose, button wording or every micro-transition where those details do not affect causality, Knowledge, custody, actor presence, resolver eligibility or later Chronicle state.

## 9. What CE / Coding should consume

CE / Coding should preserve the authored causal rails without turning them into a giant hard-coded branch screenplay.

Required separation:

`authored branch invariant`
→ `current Chronicle state`
→ `legal resolver(s)`
→ `committed factual result`
→ `visible Scene Board update`
→ `new legitimate pressures / choices / Story eligibility`.

Examples:

- `ATTACK` may authoritatively prevent the clean handoff while Battle/no-Battle, exact custody and escape are still separately resolved;
- `PICKPOCKET` may authoritatively prevent the clean handoff while detection/custody side effects remain resolver/current-state dependent;
- `OBSERVE` authoritatively allows the handoff and triggers the decoy-assassin escalation, after which CE derives the legitimate next decision situation from the resulting state;
- Pakkun remains unavailable until the later authored trigger/path makes his involvement eligible.

## 10. Reminder / anti-forgetting contract

If future Writing, CE or Coding work contradicts or forgets this branch structure, the specialist should surface the contradiction rather than silently reinterpret it.

If implementation requires information not closed here, the owner should identify the exact missing fact and return only that focused question to Stephen.

Stephen should not have to re-explain the entire Kakashi Origin to recover one missing branch detail.

## Final shorthand

> **ANBU objective: stop the package reaching the wrong hands.**

> **ATTACK = direct intervention; clean handoff prevented/interrupted.**

> **PICKPOCKET = stealth/interception; clean handoff prevented.**

> **OBSERVE = allow handoff for information; package transfers; decoy-assassin/Rogue Chūnin escalation appears; new choices open; one later path leads to Pakkun.**

> **GET CLOSER = exact continuation still requires recovered/approved authority.**

> **Story authors these causal rails. CE/resolvers realise the exact Chronicle inside them.**
