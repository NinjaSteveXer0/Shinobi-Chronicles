# Academy Kakashi Origin — Structured Autonomy Anchor Addendum: Decision-Continuation Reconciliation

**Date:** 2026-09-15  
**Owner:** Stephen / Writing — Konoha  
**Status:** **LOCKED ADDENDUM — CONSUME WITH CURRENT KAKASHI STRUCTURED AUTONOMY INVENTORY; FINAL MERGE AT ORIGIN CLOSURE**

## Purpose

This addendum reconciles the current Structured Autonomy anchor inventory with:

`Documentation/Story/Academy_Kakashi_Decision_Continuation_Matrix_and_Pickpocket_Route_Lock_2026-09-15.md`

commit `0e0f99e688701ce9985ac715aa2041b1e2b49070`.

Where this addendum conflicts with the older current inventory, this addendum is newer Story authority.

---

# 1. `AK_SA_004` supersession — Attempt Pickpocket exact route

The prior `AK_SA_004` left Pickpocket's exact resolver return open.

Stephen has now closed the high-level return families.

## Pickpocket SUCCESS

**Class:** owning resolver -> Story-fixed successful withdrawal.

Committed sequence:

`Pickpocket resolver SUCCESS`
-> `package custody = Kakashi`
-> `clean intended handoff prevented`
-> `Kakashi withdraws/disappears before AMT or Package Smuggler detect the theft`
-> `no immediate participant response can alter that completed withdrawal`
-> `no Masked Interceptor appearance`
-> `no Battle`
-> later factual debrief / ending projection.

Participant-first does not create a retroactive interception after the clean withdrawal already committed.

## Pickpocket FAILURE

**Class:** Story-fixed escalation into `BATTLE_OWNED_AUTONOMY`.

Committed sequence:

`Pickpocket resolver FAILURE`
-> `detection/escalation`
-> `Masked Interceptor makes branch-specific appearance before Battle`
-> PL Battle:

**Kakashi Hatake vs ANBU Marked Target + Package Smuggler + Masked Interceptor**

This is a **3-v-1**.

Masked Interceptor's appearance is Story-fixed for this route and is not an autonomy roll.

Once Battle begins, exact tactical actions/order are Battle-owned.

No downstream Pakkun interception is created by the original-exchange Pickpocket failure.

---

# 2. `AK_SA_022` supersession — quick-win Package Smuggler -> AMT continuation

The older inventory incorrectly stated that Pakkun should not appear on the quick-win Package Smuggler route because Kakashi does not reach AMT.

That is superseded by Stephen's exact timing clarification.

**Correct authority:**

- defeat Masked Interceptor within **1–4 turns** -> Package Smuggler remains reachable;
- defeat Package Smuggler within **1–3 turns** -> ANBU Marked Target remains reachable;
- legitimately reach AMT -> Pakkun is present under existing downstream authority.

Therefore `AK_SA_022` now includes a second continuation gate after Package Smuggler Battle victory.

Exact ordering:

`MI Battle <= 4 turns`
-> `PS catch-up remains eligible`
-> `PS Battle <= 3 turns`
-> `AMT catch-up remains eligible`
-> `AMT reached`
-> `Pakkun entry commits`
-> later AMT Battle/disposition state.

A qualifying deterministic post-Battle KILL on MI or PS must not receive an additional hidden timing penalty that silently invalidates Stephen's locked sequential triple-kill benchmark after the Battle-turn gate is satisfied.

Resolver-determined lethal attempts may still alter state according to their factual result.

---

# 3. `AK_SA_023` — generic post-resolution participant classification

**Class:** Story/CE state-classification seam before next protagonist choice.

After any Battle/resolver returns, each materially relevant participant must be classified from committed fact as:

- `CONTROLLED_DEFEATED`;
- `DEFEATED_BUT_NOT_CONTROLLED`;
- `ESCAPED_UNAVAILABLE`;
- `DEAD`;
- another explicitly authorised state if CE uses different neutral naming.

This classification determines whether the next protagonist affordance can legitimately include:

- deterministic KILL;
- resolver-determined lethal attempt;
- custody/return;
- leave/continue pursuit;
- no further action against that participant.

Participant-first autonomy happens from this committed state. It does not reroll the prior Battle result.

---

# 4. `AK_SA_024` — Take Him Down defeat package closure

**Class:** committed Battle defeat -> package-state evaluation -> due participant autonomy where still unresolved.

Locked:

- AMT defeats Kakashi;
- target custody is lost;
- package custody must consume actual committed autonomy/Battle state.

Continuation classes:

- package secured by Kakashi/Pakkun side -> AMT escapes, package recovered/retained;
- package secured by AMT -> AMT escapes with package;
- package neutral at Battle return -> due legitimate participant action resolves package custody before Story closure.

Do not select a final debrief/ending while the package is still semantically unresolved.

---

# 5. `AK_SA_025` — Secure Package 2-v-1 victory post-Battle decision seam

**Class:** post-Battle classification + participant autonomy + protagonist continuation.

Entry:

- Kakashi defeated Package Smuggler + Masked Interceptor;
- package secured;
- AMT still has a clean downstream pursuit opportunity.

Required ordering:

1. commit Battle result;
2. commit package custody;
3. classify PS/MI post-resolution states;
4. resolve due participant autonomy;
5. derive Kakashi affordances;
6. if Kakashi leaves immediately, preserve clean AMT pursuit opportunity;
7. if Kakashi takes optional post-resolution actions, commit them before recalculating continuation;
8. if AMT is legitimately reached, Pakkun entry commits.

No automatic kill/custody/release may be inferred for PS or MI.

---

# 6. `AK_SA_026` — Stop Package Smuggler victory post-Battle decision seam

**Class:** post-Battle classification.

Locked context:

- AMT escaped with package;
- Package Smuggler defeated;
- no Pakkun;
- MI unseen.

PS is classified from factual Battle state.

If controlled, default nonlethal continuation is return/custody to originating ANBU authority. A legitimate lethal affordance may appear only if state/classification permits it.

What Kakashi does to PS does not retroactively recover the escaped package.

---

# 7. `AK_SA_027` — Go After Original Target victory without package

**Class:** post-Battle classification with package/disposition non-collapse.

Locked:

- AMT defeated/captured;
- package not in Kakashi custody;
- package-secured four-way disposition menu does not become eligible as a bundle.

However:

> package failure != physical inability to perform a lethal disposition.

Therefore AMT's exact defeated/control state is classified independently.

- controlled -> deterministic Kill may be eligible under current lethal contract;
- not controlled -> any lethal intent is resolver-determined;
- default nonlethal continuation remains live return to originating ANBU authority.

Pakkun remains present until explicit departure.

---

# 8. `AK_SA_028` — Pickpocket-failure 3-v-1 post-Battle return

**Class:** `BATTLE_OWNED_AUTONOMY` -> multi-participant post-resolution classification.

### Battle victory

All three opponents are classified separately after Battle.

Do not collapse three participant states into one generic `victory` flag.

Package custody commits separately.

The next player affordances derive from the combined post-Battle state and may legitimately create mixed lethal/custody outcomes.

### Battle defeat

- no Kakashi participant custody;
- package state consumes actual resolver/Battle state;
- no post-capture menu;
- debrief records all three materially encountered actors.

The 3-v-1 is intended as a Legacy-level difficulty target but is not a Story-enforced Legacy gate.

---

# 9. Final reconciliation obligation

At Academy Kakashi Writing closure, the primary Structured Autonomy inventory must be reconciled so that:

- old `AK_SA_004` open Pickpocket wording is replaced by this exact route;
- old `AK_SA_022` terminal no-Pakkun wording is removed;
- `AK_SA_023` through `AK_SA_028` are merged or represented by equivalent machine-addressable anchors;
- no duplicate/superseded anchor survives as active Coding authority;
- Coding consumes the final reconciled inventory, not both conflicting versions blindly.

Design/authoring authority here does not claim runtime implementation or Golden GREEN.

---

## Final lock

> **Pickpocket SUCCESS commits clean package extraction and completed withdrawal before participant reaction can undo it.**
>
> **Pickpocket FAILURE commits Masked Interceptor's branch-specific entrance and a Kakashi-vs-AMT+PS+MI 3-v-1 Battle.**
>
> **MI <=4 and PS <=3 preserves the later AMT/Pakkun continuation.**
>
> **Post-Battle participant state is classified before lethal/disposition affordances are derived.**
>
> **Package state, participant state and lethal state remain independent Chronicle dimensions.**
