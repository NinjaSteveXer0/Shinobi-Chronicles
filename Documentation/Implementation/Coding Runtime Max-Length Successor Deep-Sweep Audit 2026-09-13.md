# Shinobi Chronicles — Coding / Runtime Max-Length Successor Deep-Sweep Audit

**Date:** 2026-09-13  
**Owner:** Coding / Runtime  
**Scope:** Final pre-Alpha execution + archaeology reconciliation before retirement of the old Coding workspace  
**Authority order used:** current GitHub source → durable SC/CE documentation → current closed specialist authority → recoverable prior Coding history → memory/inference  
**State boundary:** discussion/design ≠ durable authority ≠ implementation ≠ source/headless validation ≠ installed-browser validation ≠ Golden.

This audit is an execution ledger, not a design summary. It incorporates the predecessor audit, the successor activation receipt, the successor first-action card, current GitHub issue evidence, current runtime modules, current production load order, Registry/Assets cardinality authority, and the repairs made while performing this sweep.

## 1. Exact HEAD

The exact pre-document repository HEAD used for the final publication pass is:

`4e578f58706d2308478961fb45838423efcef853` — `qa: add #112 M9 causal locator runtime harness`.

The last runtime/source-changing commit before that harness is `3605d174a1df8324ce6a57ca9966e4c19f2e4604`; the last #112 static QA reconciliation before the focused harness is `fbf0ea94853471477995b7ef32f29e33d0a87909`.

The live #112 repair/reconciliation chain produced or consumed during this sweep is:

| Commit | Meaning |
|---|---|
| `00033ae0d225e221aa2ee1e6e23f86a95ac8dc91` | recover corrupted #112 Konoha World core |
| `486fab197ba9c1bb87622e33fbb8706176621e67` | harden #112 QA after recovery |
| `11477323624ace012cc67dbd6c6333e3acd747df` | gate M9 Story locator on exact veterinary lead |
| `3605d174a1df8324ce6a57ca9966e4c19f2e4604` | make #112 production load order parser-deterministic |
| `9af1fadd82e28f0edd9bde51c2d60a877ff46f53` | cover M9 causal locator gate in #112 QA |
| `fbf0ea94853471477995b7ef32f29e33d0a87909` | reconcile #112 static QA to recovered runtime + direct parser load order |
| `4e578f58706d2308478961fb45838423efcef853` | add focused executable M9 causal-locator runtime harness |

This document is documentation-only on top of that audited implementation/test baseline; therefore repository HEAD after publication is newer than the baseline named above.

## 2. Runtime snapshot

Current Alpha runtime is modular rather than concentrated in `game.js`. Production `index.html` loads `game.js` followed by specialist runtime modules. The #112 path is now parser-deterministic: `runtime/alpha-world-konoha-112-core.js` then `runtime/alpha-world-konoha-112-fix.js`, rather than the old dynamic bootstrap being the production loader. The compatibility bootstrap remains a legacy artifact but is not the production parser entry.

The current runtime tranche includes the Menma tutorial, Konoha World, browser-playability, Battle browser, browser polish, Genin roster, Journey surface, Academy Origins 32900 split package, Battle modernisation, Alpha sprint 33100, traversal bridge 33200, and ANBU/Root contained-map modules. `ALPHA_PLAYABILITY_MATRIX.txt` remains the high-level source-regression matrix; it must not be confused with an installed-browser Golden.

The predecessor successor card is intentionally stale archaeology: it records `714bb802...` / `51f6d718...` and orders #111 → #112 → #63. Those first three implementation lanes have since been consumed; its non-collapse rules remain useful, but its commit frontier is not current truth.

## 3. Issue classification

| Issue | Current Coding classification | Evidence boundary / remaining work |
|---|---|---|
| #111 Academy Menma tutorial | **COMPLETE / RECORD ONLY** | Runtime + QA consumed; 23/23 recorded QA, exact five ordinary Menma start palette, no generic fallback; issue closed. |
| #112 Konoha World package | **IMPLEMENTATION PRESENT / SOURCE REPAIRED / VALIDATION ONLY** | Corrupt tail recovered; Byakugan ID fixed; 24 rows / 13 standing / 4 pressure / 7 responsive protected; KOH-X24 bounded; M9 locator causally gated; direct core→fix production load; focused runtime harness added; fresh execution output + installed browser/Golden still required. |
| #63 Genin roster v2 | **VALIDATION ONLY** | v2 runtime, recruitment/reservation SAVE-reserves-CONTINUE-assigns semantics, save/load headless evidence and traversal bridge exist; fresh installed-browser post-Promotion v2 round-trip remains. |
| #90 M1 pre-Whisper trace | **VALIDATION ONLY** | Source/headless path exists; fresh installed browser visibility/use/persistence remains. |
| #54 World Map v2 | **VALIDATION ONLY** | Source/headless diagnostics recorded GREEN; responsive browser geometry, pointer/keyboard/focus, save/load and Golden remain. |
| #84 Konoha v3 | **VALIDATION ONLY** | Implementation/source diagnostics recorded GREEN; installed-browser marker/halo/zero-leak proof remains. |
| #52 M5 Female Operator | **VALIDATION ONLY** | 37/37 recorded diagnostics + node check; Story→Battle→same-Story browser round-trip/Golden remains. |
| #32 Chronicle Interaction + My Clan | **VALIDATION ONLY** | Implementation/headless present; stale wrapper/delegate diagnostic predicates exist as test-debt, not proof of missing semantics; browser/layout/save-load remains. |
| #105 Academy Origins | **VALIDATION ONLY + protected timing gap** | Production 32900 package covers Menma + nine non-Menma routes and recorded representative headless consequences; 10/10 installed-browser Golden pending. Obito FULL/SUBSTANTIAL/REDUCED/MINIMAL delayed-arrival timing must not be fabricated without elapsed-arrival authority. |
| #141 Alpha browser/traversal tracker | **VALIDATION TRACKER** | 33100 + 33200 source/runtime smokes recorded; target Origin→Academy team→Promotion→#63→Current Journey→#90→Whisper path requires installed-browser proof. |
| #155 ANBU/Root contained maps | **VALIDATION ONLY** | Runtime + source/static/runtime harness evidence recorded GREEN; installed browser rendering, 8+8 anchors, DOM/accessibility zero-leak and Golden remain. |
| #41 M11/M12 | **VALIDATION ONLY** | Source package and recorded source diagnostics GREEN; fresh installed browser M11/M12 stages, return/save-load/Golden remain. |
| #36 Arc-1 first real Golden | **WAITING ON VALIDATION OWNER / umbrella Golden** | Arc-1 design/durable authority/source/headless are not to be reopened merely because installed-production-browser Golden is still outstanding. |
| #44 Land of Fire ecology | **WAITING ON WORLD** | Old Fire(10) geometry is explicitly stale; do not hard-bind until World recalibrates semantics against the replacement final Fire master. |
| #121 mission-skeleton CE choice generation | **QUEUE** | Reusable CE work; do not interrupt current Arc-1 validation. |
| #151 decision-sensitive reward snapshot/debrief | **QUEUE** | Post-current Arc-1 blockers. |
| #42 arc-entry state projection | **QUEUED / WAITING ON STORY** | Do not invent Arc-2 exact caller/boundary. |
| #23 contextual Special Jōnin evidence | **QUEUE** | Not an Alpha implementation blocker until executable content consumes it. |
| #86 contextual Skills coordination | **WAITING ON OWNER / QUEUED** | Coordination lane, not an immediate Coding defect. |
| #16 portrait gate | **ASSET/CARDINALITY BLOCKER SUPERSEDED; VALIDATION ONLY** | Binding live target is 97 Characters + 18 Entities = 115; Teen Nagato retired. Independent 115/115 asset workflow evidence exists; remaining resolver/browser verification only. |

No duplicate replacement issues should be created for these lanes.

## 4. Archaeology

The predecessor durable audit is `Documentation/Implementation/Coding Runtime Final Pre-Alpha Deep-Sweep Archive Audit 2026-09-12.md` at historical audit commit `74fbe699a465ba947e7804559fc071c21532dc2a`, auditing parent `66607e8...` and then-latest runtime-bearing `51f6d718...`. It is archaeology only, not current implementation truth.

`Documentation/Coordination/Coding Runtime Successor Activation Reconciliation 2026-09-12.md` consumed that predecessor audit and explicitly declared the old Coding workspace continuity-safe at that time while leaving Alpha gates open. It also fixed the live-115 population and the Arc-2 selective-restoration boundary.

`Documentation/Coordination/Coding Runtime Successor First-Action Card 2026-09-12.md` then directed the successor to consume #111, #112, #63 in sequence and forbade unnecessary rebuilds of Battle, PL, Hosted Entity, live-115 cardinality, World Map v2/Konoha v3/My Clan foundations merely because browser Golden was pending.

The live repository now proves those activation assumptions were subsequently superseded on implementation status: #111 is closed, #112 exists and required recovery rather than first implementation, and #63 v2 exists with persistence semantics. Therefore old issue comments saying “SEND NOW because implementation absent” are stale whenever current source contradicts them.

The most important archaeological find in this sweep was not a missing design note but a believed-complete source corruption: #112’s original core was malformed from KOH-X24 onward and contained `skil_byakugan_peripheral_detail`. That defect was recovered from durable World authority rather than guessed from the damaged blob.

## 5. Implemented

The following implementation families are present in current source or have durable current evidence and are not to be rebuilt from scratch: Menma tutorial start palette; Konoha World info/marker/story-locator package; Genin v2 roster/reservation; Academy Origins; Arc-1 mission runtime through M12; Chronicle Interaction/My Clan; World Map v2; Konoha v3; Battle modernisation; browser traversal bridge; ANBU/Root contained-map runtime; current live-115 portrait mapping.

For #112 specifically, current source reuses the existing World opportunity registry and existing lifecycle/discovery/actionability/resolution/tracking setters rather than creating a second World engine. Responsive capability checks fail closed. KOH-X24 can only create the S02 suspicion trace when occurrence truth supports it; it grants no material reward, does not reveal the hidden canonical laboratory name, and does not grant access. The M9 locator now requires the committed exact lead `arc1_veterinary_ward_lead_01`; unrelated knowledge of `KON-O21` is insufficient.

## 6. Missing

No broad missing-runtime rebuild was discovered after reconciliation. The remaining production work is concentrated in evidence and owner-gated seams:

| Missing / unresolved item | Type |
|---|---|
| Fresh execution output from the reconciled #112 static QA and focused M9 runtime harness after the final recovery/load-order/M9 changes | source/headless validation |
| Installed-browser end-to-end traversal and responsive interaction proof across current Alpha | browser validation |
| Golden evidence for the Arc-1/onboarding/World/contained-map lanes | Golden |
| Fire ecology calibration against replacement Fire master | World-owned authority |
| Exact Academy Origin Obito delayed-arrival bands if still required | upstream semantic/timing authority |
| Exact contents of two historically named validation documents described in Section 14 | protected recovery gap |

## 7. Runtime validation

Recorded prior issue evidence distinguishes deterministic/source validation from browser validation. #111, #63, #41, #52, #54, #84, #105 and #155 each have source/headless or dedicated diagnostic evidence in their issue/commit history; those results remain useful unless newer source invalidates the tested surface.

For #112, older validation cannot be blindly inherited because the core and load chain changed during this sweep. The current dedicated gate `tools/qa_issue_112_konoha_world.py` checks UTF-8/C0 corruption, Node syntax for core and fix, reuse of existing World registry/runtime, schemas/manifest, exact row/pool cardinality, public/restricted Knowledge projections, hidden-name non-leakage, KOH-X24 boundaries, exact Byakugan requirement, semantic overlay classes, no presentation population, no Fire standing geometry, one-shot Ryō, no cross-domain reward mutation, responsive fail-closed semantics, withheld authoring checkpoint, locator non-teleport behaviour, direct production core→fix parser order, M1 seam, semantic refill timing, idempotent migration, M9 exact lead/committed-history/no-focus gate, and explicit browser-Golden non-claim.

The focused executable harness `tools/qa_issue_112_story_locator_runtime.js` executes the live correction module with a minimal World-runtime shim and asserts: no lead fails closed; unrelated `KON-O21` history is insufficient; uncommitted exact lead is insufficient; exact committed lead unlocks the existing locator; exact committed source-ref provenance also unlocks it; diagnostics pass; locator checks write no save; browser Golden remains unclaimed.

**Important:** the existence of these final current gates is test-definition evidence. This audit does not claim their final current output was independently executed after the last reconciliation commit unless exact execution output is subsequently attached.

The repository’s code-search index returned no `TODO` or `HACK` results during this sweep, but that search facility also failed to return known `catch` strings and is therefore not strong enough to prove global absence. Known #112 catch sites fail closed rather than silently granting capability. No claim of a repository-wide “zero silent catches” proof is made without a byte-level clone/runner pass.

## 8. Browser validation

Installed-browser proof remains the largest common open gate. The required high-value path is:

`Origin → Academy team → Promotion → #63 Genin finalisation → Current Journey → #90 three-person trace → Whisper Woods → Arc-1 onward → M11/M12 → return surfaces`.

This pass must also cover World Map v2/Konoha v3 marker discoverability, keyboard focus, responsive geometry, known-unknown zero-leak behaviour, contained ANBU/Root maps, portrait resolver behaviour, and SAVE/CONTINUE transitions.

Source/headless GREEN must not be promoted to browser GREEN merely because a function exists or a Node/static diagnostic passes.

## 9. Golden

Arc-1 Golden remains open under #36 and related specialist tickets. Current source/headless evidence means Golden should be run as validation, not used as a reason to reopen settled Story/Combat/PL/Registry architecture.

No new Golden was produced by this sweep because this environment did not provide an installed browser/computer-use surface. The audit therefore preserves existing Golden claims only where already evidenced and explicitly leaves current changed #112/browser traversal unclaimed.

## 10. Persistence

Persistence-sensitive semantics recovered/revalidated from current evidence include: #63 incomplete team reservation surviving SAVE/CONTINUE until final assignment; #112 semantic refill only after committed action; one-time #112 existing-save pressure migration receipt `world_activation_konoha_v1_pressure_migration_2026_09_12`; #155 preference/save-load harness evidence; #105 consequences avoiding direct PL mutation.

The #112 M9 gate reads only committed activity-history addresses for the exact veterinary lead, so unrelated presentation/open/render state is insufficient to reveal the M9 Main Story locator. The focused harness explicitly asserts that merely querying this locator performs zero save writes.

Fresh installed-browser SAVE/reload round-trips are still required before these can be called browser-Golden.

## 11. Cardinality

The binding live Alpha population is:

**97 Characters + 18 Entities = 115 total.**

Teen Nagato is intentionally retired. Historical `116` language is superseded and must not be revived without newer explicit Registry authority.

Durable authority is `Documentation/Registry/Teen Nagato Retirement and Live 115 Cardinality Ratification 2026-09-11.md` (`6d1c9eef...`). Subsequent recorded workflow evidence includes successful 97/97 Character, 18/18 Entity, 115/115 manifest/path/decode/dimension checks with Teen Nagato absent and resolver/no-card-fallback GREEN. #16 has been updated during this sweep to prevent a stale 116th-portrait blocker from reappearing.

## 12. Asset mapping

Character-card assets and Battle/UI portrait assets remain separate surfaces. Current Alpha asset mapping is governed by the live-115 Registry identity set, not old filename assumptions. Portrait completeness was independently workflow-validated at 115/115 after the Teen Nagato retirement/rebaseline work.

The remaining Coding responsibility is resolver/browser proof: correct physical portrait resolves for the represented identity/variant, no card-art fallback where a portrait is required, no same-person collision leakage, and no stale retired identity reference in UI projection.

Contained ANBU/Root map artwork is implementation-present, but local anchor rendering/accessibility/zero-leak remains browser-validation work; visual appearance must not be used to infer new World ontology.

## 13. Superseded assumptions

| Superseded assumption | Current truth |
|---|---|
| “#112 is not implemented” | Stale. It is implemented and was repaired during this sweep; remaining lane is validation. |
| “#63 v2 is not implemented” | Stale. Runtime/reservation/save-load semantics exist; remaining lane is installed-browser validation. |
| “116 live portraits” | Stale. 97 + 18 = 115; Teen Nagato retired. |
| “#16 is asset-blocked” | Stale. Asset/cardinality gate superseded; resolver/browser only. |
| “Old Fire(10) geometry is safe to bind” | False. Wait for World calibration against replacement Fire master. |
| “Dynamic #112 bootstrap is production load authority” | Superseded. Production parser directly loads core then fix; bootstrap is legacy compatibility artifact. |
| “Knowing KON-O21 is enough to expose M9 Story focus” | False. Exact committed `arc1_veterinary_ward_lead_01` is required. |
| “Source/headless GREEN means browser/Golden GREEN” | False by project doctrine and this audit. |
| “Discarded Arc-2 Unknown Boy alley/timed-Battle caller is active” | False. Preserve selective-restoration authority; do not resurrect discarded caller. |

## 14. Protected gaps

Two files were explicitly named as validation inputs during the max-length successor mandate but cannot be recovered from current GitHub using their exact historical paths:

`Documentation/Coordination/Runtime Validation Doctrine 2026-09-10.md`

`Documentation/GoldenGate_E2E_Test_Sheet_v1_2026-08-26.md`

Direct current-path reads return 404. Exact-path GitHub commit-history queries return `[]`. Current repository code search returns no matching renamed copy. Their exact historical contents are therefore **not recovered** and must not be silently reconstructed from general project knowledge.

This does **not** erase the state-separation doctrine because the same non-collapse boundary is independently durable in current successor coordination documents and issue evidence. It does mean the exact historical validation checklist/test-sheet content remains a continuity gap until recovered from an old chat/export/backup or explicitly superseded by a newly authored authority.

Other protected gaps remain where upstream authority is explicitly required: Fire post-replacement calibration, Academy Origin Obito elapsed-arrival reward bands if still required, and any unrecovered detailed original Arc-2 M3–M12 Story material not superseded by later selective-restoration authority.

## 15. Blockers

There is no single discovered Coding source defect comparable to the repaired #112 corruption still blocking broad implementation. Current blockers are:

| Blocker | Owner / resolution |
|---|---|
| Fresh final #112 static QA + focused M9 harness execution output after recovery/parser-order changes | Coding/runtime validation environment |
| Installed-browser consolidated traversal / responsive / accessibility pass | Coding/browser validation / #141 + #36 umbrella |
| Golden evidence for current Alpha path | validation owners |
| Fire ecology geometry calibration | World |
| Two unrecovered exact historical validation artifacts | archaeology / old workspace export / explicit replacement authority |

The absence of those validation artifacts prevents a claim that *every exact historical validation instruction* is durable outside the old workspace, even though production implementation continuity itself is substantially recovered.

## 16. Next execution order

1. Run `tools/qa_issue_112_konoha_world.py` and `tools/qa_issue_112_story_locator_runtime.js` against the current repository with Node available; attach exact output to #112. Do not close #112 if either gate is not GREEN.
2. Run the consolidated installed-browser path through #141/#36, explicitly exercising #63 reservation persistence, #90 transition, World/Konoha marker zero-leak, M11/M12 return, and current portrait resolver.
3. Convert each already-implemented open ticket from validation-only to closed only when its own browser/Golden acceptance evidence exists; do not rewrite settled implementation just to satisfy stale issue titles.
4. Keep #44 blocked until World returns replacement-Fire calibration. Keep #121/#151/#23/#86 queued unless a current Alpha consumer elevates them. Keep #42 behind exact Story boundary.
5. Recover the two exact historical validation artifacts from the old Coding workspace/export if possible. If they cannot be recovered, create an explicit new durable validation authority that intentionally supersedes them rather than pretending their contents are known.
6. After those evidence gates, rerun the final source/browser/Golden ledger and only then declare Alpha validation closure.

### Routing footer

**Coding / Runtime owns:** current-source correctness, load order, runtime integration, deterministic diagnostics, persistence implementation, browser traversal implementation, and attaching exact evidence to Coding-owned issues.  
**CE / Codex / Coordination owns:** cross-specialist doctrine/routing and explicit supersession when historical authority cannot be recovered.  
**World owns:** replacement Fire geometry/calibration and World semantic authority.  
**Story owns:** exact unresolved Story caller/timing boundaries.  
**Registry / Assets owns:** identity/cardinality authority and asset existence; Coding owns resolver/runtime projection.  
**Stephen is not the routine message bus.** Existing GitHub issues/docs are the durable handoff surface.

OLD CODING WORKSPACE ARCHIVE NOT SAFE — exact unrecovered material remains production-relevant
