# Shinobi Chronicles — Coding / Runtime Legacy Workspace Audit and Restart Baseline

**Date:** 2026-09-07  
**Status:** DURABLE ARCHAEOLOGICAL AUDIT / CURRENT-SOURCE RESTART RECEIPT  
**Owner:** Coding / Runtime  
**Purpose:** Preserve the recoverable Coding/Runtime lifecycle, classify it against current `main`, and provide a source-first restart point after the original Coding workspace reached its conversation limit.

> This document is an implementation/status receipt. It does not supersede current source, CE/SC design authority, specialist-owned contracts, or later GitHub handoffs.

## 0. Authority order and state ladder

Use this order for all future Coding work:

1. current GitHub `main` source;
2. durable CE/SC documents;
3. current specialist decisions / implementation-ready GitHub handoffs;
4. Project memory / recovered legacy chat context.

Preserve the state ladder:

**design closed ≠ implemented ≠ runtime validated ≠ Golden/regression GREEN**

A historical GREEN remains evidence about that exact artifact. It is not proof that a later repository state is GREEN.

---

# A. Current Coding baseline

## A1. Current source

Audit-time `main`:

- HEAD: `0557599d4ba2ae3981328ca4d9ec34150735ff62` — `fixed practical ui size. Needs new coding.`
- current `game.js` blob: `442d4f7612529d4e750758b416344bcd5d2aeb9d`
- current `style.css` blob: `44d27b755c41f931d556d3c3f6fa86242bf82bef`
- current `UI/practical.png` blob: `6e2967f3e0522a615097fb413a74cb4de0f51b93`

The current `game.js` is the exact Post-1969 artifact previously verified on `main`; the later HEAD commit changes the Practical raster only.

Therefore the active Coding restart baseline is:

**Post-1969 source + later repository/document/asset changes**, not Brick 90, Post-866, Post-899, Post-1060, or Post-1859.

## A2. Current runtime/regression evidence

### Post-1969 — implemented + locally runtime validated

GitHub issue #14 preserves the exact artifact fingerprint and validation receipt:

- `node --check` PASS;
- deterministic Node/V8 runtime harness PASS;
- `runAlphaMen03TutorialPerformanceSourceDiagnostics()` = 16/16 PASS;
- `runAlphaPost1969MEN03IntegrationDiagnostics()` PASS;
- Post-1939 Unknown Operative Golden remained 48/48 PASS;
- Post-1939 integration PASS;
- Post-1859 Coding-owned gate remained true;
- Registry remained 98 Characters + 18 Entities = 116;
- expected Git blob `442d4f7612529d4e750758b416344bcd5d2aeb9d` matched current source.

This is legitimate local deterministic regression evidence for the exact current `game.js` artifact.

It is **not** a current full-browser / whole-repository Alpha Golden.

### Post-1939 — implemented + locally runtime validated

Issue #13 records:

- Unknown Operative confrontation diagnostics 48/48 PASS;
- Post-1939 integration PASS;
- Post-1859 cumulative Coding gate GREEN;
- stable participant life/custody/injury persistence;
- caller return/evidence envelope preserved;
- Registry 116 preserved.

A final Chromium `--dump-dom` attempt timed out at that stage, so no full-Chromium GREEN was claimed.

### Portrait CI — current repository gate is RED/stale

Current `.github/workflows/battle-portrait-qa.yml` and `tools/qa_battle_portraits.py` still encode the historical 102-row / `Assets/Portraits/**` model. Current Assets/Registry authority is live 116 under `Portraits/`.

The latest available Battle Portrait QA run failed in the `Validate 102 ratified Battle portraits` step.

Issue #16 already owns the Coding SEND NOW correction. Do not remap portraits. Repair the QA/tooling to consume the exact ratified live-116 authority and prove decode/dimensions/path/loadability/runtime resolver/no-card-fallback.

### General regression-harness durability gap

Current tracked tree does not contain the old `package.json`/`npm test` harness used by the Post-899 integration receipt, nor a repository-level general regression artifact comparable to the historical local cumulative runners.

Many diagnostics remain embedded in `game.js`, and exact package results are preserved in issue/comments and implementation receipts, but the whole cumulative gate is not presently reproducible from a fresh current checkout through one checked-in command.

Classification: **runtime-tested work without fully durable/reproducible repository-wide regression authority**.

This is not evidence that the runtime is broken. It is a reproducibility/Golden gap.

## A3. Current live Alpha blockers

1. **Practical exact accepted physical master + downstream Coding calibration** — issue #9 remains open. The binary currently on `main` is the earlier 2,621,935-byte RGB 1536×1102 raster. The subsequently accepted runtime-neutral master is a different 3,803,339-byte RGBA binary with SHA-256 `7933d4e6b1613adaff2b39ab08a426b54e2e80ab478296d33bdc02db2247fabb`; it has not yet been transported to `main`. Coding must not recalibrate against the superseded binary. After the accepted master lands, recalibrate overlays/hitboxes and run side-by-side/browser validation against Exams.
2. **Live-116 portrait binary/runtime QA** — issue #16. Mapping/remap authority is closed; QA tooling is stale and current CI is not GREEN.
3. **Remaining 33 Origin narrative/world `sourceOccurrenceId` boundaries** — issue #11. Coding must not infer them from contract IDs, scene IDs, prose, branch positions, or naming conventions.
4. **Whisper Woods major-contact Story Scene caller** — issue #17 created by this audit. The Unknown Operative Combat package is implemented/validated, but `scene_arc1_m1_whisper_major_contact` remains deliberately external/unregistered by Coding until Writing publishes the causal Story contract and participant/transition binding.
5. **Field Readiness authored field/map/story orchestration + end-to-end Golden** — backend/consumer semantics are closed and Coding-reported ready; current UI contract still explicitly leaves final field content and SC-specific end-to-end Golden validation open.
6. **Final Alpha Golden/freeze gate** — must run after the remaining production callers/assets are integrated. Do not equate generic/runtime package Goldens with complete Alpha-flow GREEN.

---

# B. Recovered legacy Coding/Runtime contributions

The original Coding workspace cannot be reconstructed message-for-message. Four earlier programming chats were referenced by the surviving archaeology but their raw histories were not fully accessible. The following is the maximum high-confidence recovery from surviving source checkpoints, Library artifacts, Git history, durable receipts, issue comments and current-source lineage.

## B1. Brick-90 era — historical foundation / regression genealogy

Recovered legacy source and audit material establish Brick 90 as a major pre-Shadow-Realm safety checkpoint, not current authority.

The master regression used an outer safety snapshot and chained individual diagnostic families covering, among other things:

- Equipment;
- disciplines/training;
- progression gating and transitions;
- inheritance/reset/application/runtime sync;
- atomic rollback;
- completion controller/path/session/UI/execution;
- lifecycle integration and safeguards.

Important reusable testing doctrine:

- a master test must preserve and restore outer runtime state;
- individual suites should remain independently runnable;
- diagnostics must leave no residue;
- an old GREEN is evidence for the tested artifact, not permission to skip rebasing on new source.

Classification: **historical reinforcement only**, except the regression-doctrine lesson which remains useful.

## B2. Post-866 deep-sweep state — historical runtime evidence

A recovered pre-Alpha audit reran the actual Post-866 artifact under a browser/localStorage shim:

- `node --check` PASS;
- `runAlphaPost866MonsterDiagnostics()` PASS;
- MASTER true.

At that point the generic architecture already included Battle participant/runtime state, Effective State, Conditions, source ownership/provenance, Items, Equipment, acquisition, six-slot formation, scoped history, Story Scene runtime, World Event/Hotspot runtime, boss staging, persistence, and Battle-local observer-relative historical references.

The major finding even then was that **generic architecture was ahead of production-content integration**.

Classification: **historical reinforcement only**. Never downgrade current source to Post-866.

## B3. Post-899 integration receipt — durable historical implementation proof

The durable Post-899 receipt records a locally GREEN 25-gate cumulative build including syntax/headless/static checks, battle variants, Special Jōnin catalogue, duel/world-event/fight-event/consequence/formation/deterministic rerun/trade persistence/activity log, Chūnin guard and bond behavior.

It also proves that later Post-872 statements such as “Special Jōnin 11/33 Coding population pending” were superseded by implementation.

Classification: **already durable historical implementation/runtime evidence**, superseded as a restart baseline by current source.

## B4. Field Readiness / Promotion lifecycle

Legacy Coding work after Post-899 built and tested the reusable Field Readiness assessment consumer, state/evidence bridge, Battle return path, save/reload stability and Arena→Promotion integration groundwork. Recovered legacy chat/source checkpoints place this work through the Post-1049 range.

Current durable UI authority independently says:

- Rank/Combat semantics are closed;
- reusable consumer is Coding-reported ready;
- Arena→Promotion is the Alpha entry route;
- field/map/story orchestration and end-to-end Golden remain.

Preserve non-collapse:

- Battle result ≠ Promotion result;
- UI entry ≠ assessment authority;
- assessment inspection ≠ committed attempt;
- personal performance ≠ protective performance;
- failed Promotion ≠ erased attempt;
- Battle return must resume the same assessment attempt/context.

Classification: **core implementation already present / historical runtime evidence; Alpha orchestration + final Golden unresolved**.

## B5. Entity ontology / Registry / representation

The 1050–1060 era reconciled stable Registry identity from collectible-card folder/classification changes. Current Git history contains the 1060 lineage, and current durable Entity ontology explicitly preserves:

- stable Registry ID ≠ folder/category;
- Entity ≠ Summon;
- collectibleCard path ≠ `uiPortrait` path;
- folder/category ≠ Battle lifecycle;
- source addressability may exist without Entity registration;
- Hosted Entity ≠ independent participant/turn/PL by default;
- Tailed Beast family ≠ universal Summon lifecycle.

Current live Registry/portrait authority has since moved from 102 to 116. Any legacy 102 count is historical only.

Classification: **already durable/current semantic boundary; old count superseded**.

## B6. Battle runtime and source ownership

Recovered legacy runtime evidence repeatedly exercised:

- underlying Battle PL distinct from temporary capacity;
- Stamina interaction without treating Stamina as hidden Battle-capacity scaling;
- Conditions with establishing provenance;
- source-owned runtime states/packages;
- action opportunities;
- area/guard/control state;
- source-only participants that do not automatically receive turns/PL bars;
- attached Entity actions consuming the controller Character opportunity where authored;
- no automatic Entity PL transfer;
- staged boss transitions preserving historical identity while clearing stale runtime packages;
- invalid selection creating no false action/history;
- item preflight failure consuming no quantity and post-consumption failure refunding where required.

Current CE Combat Recovery Addendum preserves the reusable architecture:

`REQUEST → CONTEXT SNAPSHOT → RESOLVER → OUTCOME → CONSEQUENCES → CHRONICLE RECORD`

and:

**historical cause ≠ runtime ownership**  
**Effective State = projection, not competing storage**.

Classification: **already durable/current doctrine; historical runtime reinforcement**.

## B7. Exact-instance Equipment / Inventory persistence

Legacy work hardened:

- item definition ≠ persistent Inventory instance ≠ equipped instance;
- exact `instanceId` persistence;
- no fallback from missing instance identity to generic item ID;
- one weapon slot / explicit unequip / active-Battle mutation lock;
- source/package idempotence;
- persistent throwing-weapon non-consumption where authored;
- Ninja Wire tool semantics;
- no invented durability system.

This is important because a convenience fallback from `instanceId` to `itemId` would contaminate save/load ownership and provenance.

Classification: **already implemented/durable historically; preserve regression coverage**.

## B8. Origin → Active Konoha history boundary

Legacy Coding and later CE/Writing archaeology converged on a real history-scope transition:

Origin selection/prologue history
→ authoritative continuity transition
→ `YOUR CHRONICLE BEGINS` presentation
→ Active Konoha continuity
→ exactly two teammate acquisitions/formation.

Preserve:

- continuity transition ≠ continuity presentation;
- Origin history ≠ Active-Konoha history;
- unselected teammate acquisition ≠ retroactive execution of that teammate's Origin;
- formation ≠ fabricated Shared History/Memory/Knowledge;
- active-Konoha boundary commits exactly once;
- presentation cannot commit it.

Classification: **already durable architecture; current per-Origin production/source integration remains partly open through issue #11**.

## B9. Story / World / Event / caller-return architecture

Later current-source lineage hardened several legacy return-path risks:

- World opportunities carry an explicit Battle caller envelope;
- `CLAIM ≠ CONTINUE`;
- generic overlay close must not discard an authored caller;
- Battle return restores the caller-owned context;
- current World/Event state is persisted separately from committed history;
- on return, observer-safe opportunity truth is recomputed from current authoritative state instead of blindly restoring a stale presentation snapshot;
- Story Scene → Battle → consequence → same continuing Story/Environment context is the intended lifecycle;
- visual battlefield/environment damage ≠ persistent world damage unless authoritative consequences commit it.

Classification: **already durable/current implementation architecture**.

## B10. MEN-03 sourceOccurrence / consequence address

Post-1969 closes a particularly important provenance boundary:

- `sourceOccurrenceId = combat_academy_menma_tutorial_performance_resolved`;
- `consequenceContractId = academy_menma_tutorial_performance_evidence`;
- source occurrence ≠ consequence contract ≠ Battle occurrence ≠ Action occurrence;
- supporting occurrence IDs remain ancestry/evidence;
- failed/non-completed attempts mint no aggregate source;
- legitimate retry may commit once;
- save/load/retry reuses the same persisted source/consequence address;
- performance evidence does not directly mutate Stats/PL.

Classification: **implemented + locally runtime validated + current**.

## B11. Unknown Operative identity/projection/life-state package

Post-1939 closes another major boundary:

- stable person: `arc1_m1_unknown_operative`;
- observer projection: `observer_projection_unknown_operative`;
- player-facing concealed label: `UNKNOWN OPERATIVE`;
- presentation projection never becomes life/custody/injury key;
- later reveal cannot mutate stable historical identity;
- Kill intent ≠ death consequence;
- Detain intent/Battle victory ≠ custody;
- Story supplies exact active participants/sides rather than inferring from team membership;
- valid escape/custody/death facts persist through authoritative consequence state;
- caller return/evidence envelope survives Battle.

Classification: **implemented + locally runtime validated + current Combat package; Story caller still unresolved in issue #17**.

## B12. Determinism / randomness / replay doctrine

Recovered Coding rules repeatedly converged on:

- semantic/causal eligibility before RNG;
- RNG may select only among eligible candidates and must never manufacture eligibility;
- failed invalid actions must not consume opportunity/history;
- retry/save/load must not reroll a committed/snapshotted decision merely because presentation/runtime was reconstructed;
- prepared Battle identities/state are snapshotted at the legitimate Battle boundary;
- exact source/package/occurrence IDs support dedupe and replay;
- diagnostic runners should restore state exactly and create no residue.

Current CE strategy extends this post-Alpha toward proof-carrying deterministic replay with decision IDs, RNG stream/seed identity, eligible candidate sets and reason traces.

Classification: **current doctrine + post-Alpha hardening opportunity**.

## B13. Knowledge / provenance / observer semantics

Legacy Coding/Combat archaeology established and current CE documents preserve:

- authoritative truth ≠ observer Knowledge ≠ presentation;
- authoritative occurrence ≠ observer-accessible historical reference ≠ interpretation;
- scope precedes fidelity; fidelity never widens scope;
- communication source/addressability ≠ physical participation;
- generated/presented text does not itself become authoritative history;
- Knowledge acquisition requires legitimate source/carrier/provenance;
- equivalent information reached by different causal routes need not create equivalent event/relationship history;
- source-only causal identity ≠ Entity registration.

Classification: **already durable CE doctrine; historical reinforcement only**.

## B14. Temporary/test assumptions that must not become production authority

Recovered archaeology explicitly identifies **Academy Boss Dave** as a temporary development joke/test placeholder. It must not become SC/CE production canon without a separate explicit decision.

Likewise do not resurrect:

- old five-Origin authority;
- 97-production-identity count;
- `firstTeamFormation` as current production semantic authority;
- deleted `fallen_hokage` / `divided_vessel` identities;
- Perfect Susanoo as Fallen Sasuke stage 2;
- old Complete Kurama Stat permutation;
- old five-action Kisōgan draft;
- automatic Reaper meta-history flags;
- `kikaichu_colony` Entity registration;
- invented Scene Environment paths;
- old `Assets/Portraits` 102-wide runtime authority;
- a convenience ontology such as a second `source_only_bound_collective_companion` category merely because old implementation wording used it.

Classification: **superseded / do not revive**.

---

# C. Drift, contradictions and lost-work risks

## C1. Portrait QA authority drift — ACTIVE

**Current authority:** 116/116 approved runtime portrait mapping under `Portraits/`; no remap required for Alpha.  
**Current QA tooling:** 102 expected, historical `Assets/Portraits/...` parsing/path watch.  
**Observed CI:** failed.

This is a genuine contradiction between current presentation/Registry authority and Coding QA infrastructure.

Owner/action: issue #16.

## C2. Practical physical-master drift — ACTIVE

HEAD's Practical raster is not the subsequently accepted exact master. Because `game.js` and `style.css` were unchanged in the asset-only HEAD commit, Coding integration is correctly not claimed.

Do not calibrate against the superseded RGB binary merely because it exists on `main`.

Owner/action: issue #9, then downstream Coding handoff.

## C3. Post-872 reconciliation contains stale status snapshots

`Documentation/PL_Registry_Rank_Post872_Reconciliation.md` still reports:

- Special Jōnin 11/33 Coding population pending;
- 102 portrait resolver/QA pending.

The Special Jōnin implementation was later consumed by Post-899. Portrait authority later moved to live 116.

Treat the Post-872 freeze section as a dated historical snapshot, not current status authority.

## C4. Origin cross-workspace matrix predates current MEN-03 implementation

The matrix correctly preserved non-collapse at its date, but MEN-03 is now implemented + locally validated on current source. Use issue #14/current source for MEN-03, and issue #11 for the remaining 33.

## C5. Development milestones file is historical/incomplete

`Documentation/Development_Milestones.md` records early Activity Engine milestones but does not represent the later Coding lifecycle. It must not be used as the restart baseline.

## C6. Regression evidence is fragmented

Strong local deterministic diagnostics exist, but current proof is spread across:

- embedded `game.js` runners;
- dated implementation receipts;
- issue comments/artifact fingerprints;
- one portrait-specific GitHub Action.

There is no single current checked-in Alpha regression command proving the complete mandatory flow from a fresh checkout.

Risk: future Coding may either overclaim GREEN from a narrow suite or discard valuable diagnostics because their invocation genealogy is unclear.

## C7. Story package vs Story caller non-collapse

Unknown Operative Combat implementation is done. The authored major-contact Story Scene remains external.

Risk: a future coder could see the implemented encounter package and wire it directly from the map, silently bypassing Story causality/participant autonomy.

Issue #17 now records the missing owner handoff.

## C8. Registry/representation inference risk

Current ontology and portrait work makes a recurring historical failure mode explicit:

- physical file existence must not create Registry identity;
- collectible-card folder must not create lifecycle semantics;
- stable ID must not be renamed to match files;
- `uiPortrait` must not fall back to a collectible card;
- source addressability must not force Entity registration.

The stale portrait QA script is a concrete example of why path-era assumptions must not become semantic authority.

## C9. Historical regression checkpoints can become accidental production authority

Brick 90, Post-866, Post-899, Post-1049 and Post-1060 are valuable genealogy. None is a rollback destination unless current source is proven corrupt and a deliberate recovery process selects it.

Future Coding must start at current `main`, establish current source fingerprints, then debug forward.

---

# D. Alpha actions — genuinely required only

## D1. Complete existing cross-specialist blockers; do not invent their inputs

- #9 UI / Assets — land the exact accepted Practical RGBA master, then create downstream Coding handoff.
- #11 Writing — publish the remaining 33 Origin sourceOccurrence boundaries, routing World/Event-owned rows directly.
- #16 Coding — repair live-116 portrait QA/tooling with no remap/path inference and obtain GREEN.
- #17 Writing — publish the Whisper Woods major-contact Story Scene runtime contract and direct downstream Coding handoff.

## D2. After #9 returns to Coding

Recalibrate only Practical-specific geometry/overlays/hitboxes against the accepted 1536×1102 master. Preserve Exams and Practical semantics. Run side-by-side browser validation at the same viewport plus responsive/hitbox checks.

## D3. After #11 returns to Coding

Implement the exact returned `sourceOccurrenceId + consequenceContractId` matrix. Preserve dedupe, retry/save-load idempotence, failed-attempt non-history and source-owner boundaries. Do not normalize names into IDs.

## D4. After #17 returns to Coding

Register/wire the exact Story Scene and its World→Story→Battle→Story return flow. Consume the existing Unknown Operative package; do not duplicate Combat rules in Story or map code.

## D5. Field Readiness final production flow

Finish only the remaining authored field/map/story orchestration and player-facing integration needed by the existing Arena→Promotion contract. Do not reopen Rank/Combat semantics. Then execute the SC-specific end-to-end Golden.

## D6. Establish the final Alpha Golden/freeze receipt

At minimum execute semantic end-to-end paths that prove:

- Origin selection → selected Origin only → continuity transition exactly once → exactly two teammates → Active Konoha;
- no retroactive Origin/Shared History/Knowledge fabrication;
- Menma tutorial/MEN-03 source idempotence survives save/load/retry;
- World/Story caller return resumes the same occurrence/context;
- Unknown Operative Kill/Detain/escape/life-state paths preserve stable identity and no false history;
- Field Readiness Arena entry, attempt lifecycle, Battle return and Promotion non-collapse;
- exact 116 Registry/portrait resolution with no card fallback;
- Practical final geometry/hitboxes/browser presentation;
- invalid actions and failed preflights do not consume or mint history;
- Equipment exact-instance persistence remains intact.

The final freeze receipt must name the exact commit/blob tested and the exact runner/browser environment.

---

# E. Post-Alpha / Chronicle Engine record

No new CE implementation should block SC Alpha merely because these ideas are strategically valuable.

## RECORD ONLY — already substantially captured in current CE doctrine

- source occurrence ≠ consequence transaction ≠ downstream history projection;
- historical cause ≠ current runtime ownership;
- Effective State as projection, not competing persistence;
- observer-safe projection ≠ stable world identity;
- world truth ≠ observer Knowledge ≠ presentation;
- communication source ≠ physical participant;
- information equality ≠ event equivalence;
- invalid selection ≠ committed history;
- participant presence ≠ UI slot type;
- relationship consequences should retain reasons/evidence rather than universal scalar arithmetic;
- current opportunity/presentation may be recomputed from authoritative state while committed history remains stable.

## QUEUE — reusable CE hardening after Alpha

1. **Deterministic replay receipts:** persist/inspect decision ID, RNG stream/seed identity, eligible candidate set, exclusions/reason codes, chosen candidate and loser disposition where randomness materially matters.
2. **Proof surfaces:** Eligibility Proof, Projection Proof, Contention Proof, Knowledge/Provenance Trace, Shared-History Trace, Object Provenance Trace, Legacy Transmission Trace and Consequence Receipt.
3. **Institutional knowledge separation:** individual Knowledge ≠ institutional/archive/public/restricted Knowledge; role may grant Access without personal memory/Competence/Power.
4. **Opportunity lineage:** explain why a possibility resolved, expired, became latent, deferred, displaced, transformed, produced a successor or became terminally lost.
5. **Generational epistemic transmission:** descendants inherit facts/secrets/debts/obligations only through explicit testimony, archives, objects, roles, oral tradition or other causal transmission—not lineage magic.
6. **Generated-dialogue write-back contract:** generated/contextual expression remains a consumer; an actually resolved communicative action may create an authorised communication occurrence and downstream Knowledge/Memory/Relationship evidence only through production authority.
7. **Regression harness productisation:** promote the useful old outer-snapshot/no-residue pattern into a repository-reproducible CE/SC test architecture rather than relying on chat-era ad hoc runners.

## NO SEND NOW CE dependency from this audit

The reusable insights above are already recorded or safely post-Alpha. None requires a new CE implementation before the current SC Alpha blockers can close.

---

# F. Durability actions

## Completed by this audit

1. Created this current-source restart/audit document so the sealed Coding chat is no longer the only place containing the lifecycle classification.
2. Created GitHub issue **#17** to Writing for the missing Whisper Woods major-contact Story Scene caller rather than asking Stephen to relay it.
3. Existing SEND NOW handoffs remain correctly owned by **#9**, **#11** and **#16**; no duplicates created.

## Required follow-up documentation / repository work

1. **Portrait QA:** update the checked-in QA script/workflow only after consuming the exact authoritative live-116 manifest structure. Watch the actual approved `Portraits/**` surface; remove 102-era assumptions; record a 116/116 GREEN receipt in #16.
2. **Practical:** when the accepted RGBA binary lands, issue #9 must record exact committed blob/dimensions/QA and create the Coding handoff before runtime calibration begins.
3. **Origins:** issue #11 must publish a complete 33-row source binding matrix and then create one implementation-ready Coding issue.
4. **Whisper Woods Story:** issue #17 must return the authored scene/participant/transition contract to Coding.
5. **Regression:** before Alpha freeze, commit or otherwise durably define a reproducible current-checkout Golden runner/receipt. Preserve embedded diagnostic functions but do not rely on inaccessible chat instructions to know how to invoke the whole gate.
6. **Status-doc hygiene:** future owners should add explicit supersession notes to dated status snapshots when later implementation changes their freeze-status sections. In particular, Post-872's Special-Jōnin/102-portrait status and the older Origin matrix should not be read as current state.

---

# Clean restart point for the next Coding / Runtime workspace

Do **not** begin from Brick 90 or any recovered ZIP/chat checkpoint.

Start with:

- GitHub `main` current HEAD;
- verify `game.js` blob against the latest implementation receipt/handoff;
- inspect open Coding/cross-specialist issues;
- run the narrowest relevant current diagnostics before editing;
- preserve current non-collapse contracts;
- implement only authority-complete work;
- after each source patch, record exact commit/blob + source validation + runtime validation + Golden state separately.

At audit time the executable Coding baseline is the **Post-1969 `game.js` blob `442d4f7612529d4e750758b416344bcd5d2aeb9d`**, with later non-code repository changes on top.

Immediate Coding-owned work available without inventing specialist content is primarily **issue #16 portrait QA/tooling repair**. Practical, the 33 Origin source bindings, and Whisper Woods major-contact Story registration must wait for their existing specialist handoffs to return implementation-ready authority.

**Never treat recovered memory as a reason to overwrite newer source.**
