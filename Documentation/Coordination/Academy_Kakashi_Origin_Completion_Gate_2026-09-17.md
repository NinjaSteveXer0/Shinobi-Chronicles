# Academy Kakashi Origin — Completion Gate

**Date:** 2026-09-17  
**Owner:** CE / Codex / Coordination  
**Status:** **BINDING ALPHA COMPLETION GATE**  
**Applies to:** Academy Kakashi Origin implementation under Coding / Runtime #188 and final Origin validation under #105  
**Primary priority:** **FINISH SHINOBI CHRONICLES ALPHA**

---

## 1. Purpose

This gate exists to prevent Academy Kakashi from being called `ready`, `complete`, `test-ready`, `implemented`, or otherwise treated as finished while the installed player path still contains stale runtime behavior, disabled authorised choices, missing terminal scenes, missing rewards, broken Battle return, mixed-generation code, or other known defects.

The completion standard is binary:

> **Academy Kakashi is not complete until every item in this gate is satisfied and the installed-browser path is approved by Stephen.**

Source/headless/CI GREEN is necessary evidence. It is not completion.

No successor Origin implementation lane may treat Kakashi as the proven shared-runtime benchmark until this gate is GREEN.

---

## 2. Mandatory end-to-end player path

The installed production path must prove, from a clean/relevant state:

`select academy_kakashi`
-> `correct current authored prologue`
-> `current Scene Board presentation`
-> `all authorised player choices available when eligible`
-> `committed intent before factual result`
-> `correct factual resolver / deterministic Story occurrence`
-> `Battle where authorised`
-> `Battle interaction and result`
-> `CLAIM`
-> `same Kakashi Story occurrence resumes`
-> `post-Battle participant/object state commits`
-> `authorised continuation choices/scenes`
-> `ANBU reporting/debrief scene`
-> `Pakkun departure where factually due`
-> `Minato / Hokage Administration private evaluation cutaway where authorised`
-> `terminal factual debrief`
-> `Kakashi Origin reward/development projection`
-> `read-only Origin Chronicle Receipt`
-> `YOUR CHRONICLE BEGINS`
-> `exactly two legitimate Academy teammates`
-> `first-Konoha/tutorial boundary`
-> `ordinary Academy free play`.

Any broken required link keeps this gate RED.

---

## 3. Choice-completeness gate

Coding must reconcile final current Writing/CE authority against the actual production choice surfaces.

Required:

- every current authorised Kakashi option is visible and actionable when its factual eligibility predicates are satisfied;
- no current authorised option remains disabled because a superseded guard, stale scene graph, old presentation branch, old cache generation, or obsolete runtime owner still controls the browser;
- no superseded legacy option is re-exposed merely to increase button count;
- unresolved factual authority remains fail-closed only at the exact unresolved seam;
- player-facing labels never manufacture factual outcomes.

At minimum the current equivalents of these families must be proven against current authority:

- opening `GET CLOSER`;
- opening `ATTEMPT PICKPOCKET`;
- opening Attack / `STRIKE BEFORE HANDOFF` family;
- Observe / handoff continuation family;
- `DEAL WITH HER FIRST, THEN CHASE THE PACKAGE`;
- `GO FOR THE PACKAGE`;
- sequential MI -> Package Smuggler -> AMT continuation;
- post-Battle participant disposition/lethal choices where state permits them;
- downstream AMT / Pakkun decisions;
- final report/debrief progression.

A stale four-choice/five-choice mismatch, disabled closed choice, or missing authorised continuation keeps this gate RED.

---

## 4. Battle gate

Where current authority calls Battle, prove:

- correct opposition/composition;
- correct caller provenance;
- current Kakashi Battle capability/skill authority is live;
- obsolete Kakashi Battle skill bindings are absent when superseded;
- hover/click/action interaction works in the installed production route;
- result provenance returns correctly;
- `CLAIM` resumes the exact same Story caller/occurrence;
- no generic Combat Arena fallback swallows a valid Kakashi Story continuation;
- Battle victory does not itself manufacture Story success, package custody, death, participant custody, or terminal reward entitlement;
- post-Battle participant classification is independent and fact-based.

Any Battle route that exits to the wrong caller or requires a stale fallback keeps this gate RED.

---

## 5. Object / participant factual-state gate

Required:

- package custody changes only through exact committed authority;
- participant defeat does not imply death/custody/control;
- post-Battle classifications consume committed facts;
- `AK_SA_033 / kak_seq_secure_package_after_ps` is consumed where eligible on the sequential Package Smuggler victory route;
- PS Battle victory itself does not directly transfer package custody;
- package recovery and exceptional timing remain independent reward predicates;
- Pakkun temporary participation does not create ownership/acquisition/assignment/name Knowledge;
- save/load/retry does not reroll or duplicate factual occurrences.

Material unresolved factual state keeps terminal closure fail-closed and therefore keeps this gate RED.

---

## 6. Terminal Scene Board gate

The end of Kakashi's Origin must play as authored Scene Board presentation rather than raw debug/admin/placeholder output.

Required:

### ANBU reporting / debrief
- correct authorised reporting/debrief sequence;
- approved current environment projection;
- correct actor/speaker projection;
- dialogue vs narration/action classified correctly;
- no invented identities, Knowledge, custody, package recovery, or participant outcome;
- no stale superseded dialogue remains in the live route.

### Pakkun departure
- appears only where committed state says Pakkun is present and departure is due;
- does not create ownership/acquisition/assignment/reciprocal name Knowledge.

### Minato / Hokage Administration cutaway
- uses current approved Minato presentation authority/assets;
- remains a private cutaway unless newer durable authority explicitly changes that;
- does not fabricate a Kakashi-Minato meeting/dialogue/Knowledge transfer;
- reads committed history rather than mutating it.

A missing, placeholder, stale, or semantically fabricated terminal scene keeps this gate RED.

---

## 7. Reward / development gate

Kakashi Origin rewards must be consumed from exact terminal factual entitlement through the existing canonical reward/development owners.

Required:

- 34800/current successor reward adapter is actually invoked by the production terminal path, not merely loaded or present in source;
- correct Ryō predicates and totals;
- correct item predicates/grants;
- correct action-linked development/progression consequences;
- no unintended grants;
- no grant directly from Battle victory unless an exact current authority explicitly makes that fact part of terminal entitlement;
- source-scoped idempotence;
- save/load/retry/reopen does not duplicate grants;
- Chronicle Receipt reads reward history but does not grant again;
- installed browser shows/reflects the actual canonical reward result.

Placeholder rewards, zero/absent rewards where entitlement exists, duplicate rewards, or source-only reward modules keep this gate RED.

---

## 8. Chronicle Receipt / completion gate

Required:

- Receipt is a read-only projection of committed Origin history;
- no reroll/recommit/inference from labels;
- exact committed decision/outcome/package/participant/reward history is represented according to current authority;
- Continue from Receipt reaches `YOUR CHRONICLE BEGINS` at the authorised point;
- Origin completion does not bypass required terminal facts/scenes/rewards;
- the player receives exactly two legitimate Academy teammates under current onboarding authority;
- Origin history remains distinct from Active-Konoha Shared History;
- first-Konoha/tutorial transition is correct;
- ordinary Academy free play is reached only after the required completion receipts.

Any incorrect terminal transition keeps this gate RED.

---

## 9. Canonical-owner / stale-code gate

Before Kakashi may be declared complete, Coding must prove the browser is not still consuming a superseded semantic owner for any Kakashi-critical responsibility.

For each relevant responsibility, identify the canonical live owner and ensure superseded owners are either:

- de-loaded/retired; or
- explicitly proven non-authoritative temporary compatibility with a documented retirement condition that does not alter the tested path.

Critical responsibilities include:

- Story choice eligibility;
- factual resolver/result commit;
- Battle caller/return;
- participant/object classification;
- Scene Board projection;
- terminal debrief;
- rewards/development;
- Chronicle Receipt;
- Origin completion/continuation;
- production delivery/load generation.

New correct code sitting beside stale live code is not completion.

---

## 10. Production delivery parity gate

Required:

- production loader chain points to the same semantic generation tested by QA;
- no stale parent loader/compatibility path silently selects older behavior;
- cache/version advancement is coherent across the coupled Kakashi delivery family;
- direct-loaded test success is not used as proof if production loads something else;
- browser replay is performed against the current released generation.

Mixed-generation delivery keeps this gate RED.

---

## 11. Save/load / idempotence gate

At minimum prove relevant Kakashi state survives save/load without semantic drift:

- unresolved/current choice state;
- committed intent/results;
- Battle return/caller continuation where applicable;
- package/participant factual state;
- occurrence-scoped transient Battle state where required;
- reward/development receipts;
- terminal/debrief/Receipt completion;
- no duplicate grants/history;
- no rerolls caused by presentation reopen/refresh.

A reload that changes history or duplicates consequences keeps this gate RED.

---

## 12. Validation levels — hard separation

Kakashi status must always state the highest proven level:

1. implemented;
2. source/component validated;
3. integration validated;
4. production-released / loader-parity proven;
5. installed-browser validated;
6. Stephen-approved Golden/regression GREEN.

No lower level may be described as Kakashi complete.

`CI GREEN` while the installed browser is RED means **Kakashi RED**.

---

## 13. Mandatory browser acceptance

Stephen is the final installed-browser acceptance authority for this Origin.

Coding may ask for the final replay only when:

- all known source/integration defects are fixed;
- all required code is committed;
- production delivery parity is proven;
- no known stale owner remains on the tested path;
- focused and broad relevant regressions are GREEN.

The final replay must exercise enough branches to prove the current authorised choice families and terminal route, not merely one happy-path screen.

If Stephen finds a defect, Kakashi returns to RED and Coding resumes under #219 until the defect is repaired and replayed.

---

## 14. No advance-on-partial-completion rule

> **Coding / Runtime may not treat #188 as complete, may not treat Academy Kakashi as the proven shared-Origin benchmark, and may not advance the Origin implementation lane on the premise that Kakashi is finished until every applicable gate item above is GREEN and Stephen has approved the installed-browser result.**

Work on unrelated non-blocking tasks may continue only where it does not replace or obscure this Alpha priority.

The purpose of this rule is not to trap the project on Kakashi indefinitely. The purpose is to force a binary finish: repair the shared runtime once, prove Kakashi fully, then propagate the proven path to the remaining Origins.

---

## 15. Completion evidence required on #188

Before #188 can close as completed, its final completion evidence must include:

- exact HEAD;
- exact implementation/retirement commits;
- canonical-owner summary;
- stale live owners de-loaded/retired;
- exact choice inventory status;
- exact Battle/caller-return proof;
- exact package/participant factual-state proof;
- exact terminal Scene Board proof;
- exact reward/development proof;
- exact Chronicle Receipt/completion proof;
- save/load/idempotence proof;
- focused QA results;
- broad integration/#141 result;
- production loader parity evidence;
- installed-browser replay result;
- explicit `Kakashi Browser Golden: GREEN` only after Stephen approval.

---

# Final lock

> **Academy Kakashi Origin is complete only when the current authorised experience works end-to-end in the installed production browser, every applicable item in this gate is GREEN, no stale semantic owner is still controlling the tested path, rewards and terminal scenes actually execute, and Stephen approves the result. Anything less is incomplete.**

**Stephen relay:** NONE. GitHub is the durable authority.
