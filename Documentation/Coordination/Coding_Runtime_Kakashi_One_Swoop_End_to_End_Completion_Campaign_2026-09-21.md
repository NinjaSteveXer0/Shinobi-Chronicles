# Shinobi Chronicles — Coding / Runtime Kakashi One-Swoop End-to-End Completion Campaign

**Date:** 2026-09-21  
**Owner:** CE / Codex / Coordination  
**Status:** **BINDING KAKASHI COMPLETION EXECUTION MODE — ACTIVE**  
**Implementation lane:** GitHub #188  
**Standing Coding protocol:** GitHub #219  
**Completion gate:** `Documentation/Coordination/Academy_Kakashi_Origin_Completion_Gate_2026-09-17.md`  
**Primary priority:** **FINISH SHINOBI CHRONICLES ALPHA**

## 1. Purpose

Academy Kakashi has remained in an implementation/browser-repair loop for too long.

The problem is no longer missing Writing.

Current authority is explicit:

`Documentation/Story/Academy_Kakashi_Origin_100_Percent_Writing_Closure_2026-09-20.md`

Status:

**100% CURRENT ROUTE / VARIATION COVERAGE**

Therefore Coding / Runtime is now authorised and required to treat Kakashi as one finite end-to-end completion campaign rather than a sequence of disconnected micro-fixes.

Canonical objective:

> **Wire the current authorised Kakashi Origin from selection to ordinary Academy free play, across every currently reachable route family, remove conflicting/stale runtime ownership, debug all discovered implementation defects, prove the complete route matrix at source/integration level, then ask Stephen for one consolidated installed-browser acceptance pass.**

This is called **ONE-SWOOP MODE**.

"One swoop" means **one sustained completion campaign**, not one literal commit and not one blind giant rewrite.

---

## 2. Current feasibility decision

**YES — this is now feasible.**

Why:

- Writing is 100% closed for current Kakashi paths and meaningful factual variations.
- Current #188 latest runtime receipt reports **no downstream owner dependency**.
- CE completion semantics are already durable.
- Rewards/development, custody, Pakkun, terminal report, Minato evaluation, Chronicle Receipt and first-Konoha continuation all have current authority.
- Current automated Kakashi / pre-Alpha lanes are already capable of proving large integrated portions of the path.
- The remaining work is implementation ownership, wiring, consolidation, traversal proof and installed-browser acceptance.

Coding must not use "I need more Story" as a default stop condition.

If a real contradiction appears, route only the exact contradiction and continue all nonblocked work.

---

## 3. Current branch / delivery reality

At the time this campaign is published:

production `main` is:

`f1e01e2169bf58bb66ad9dd087cda0cf4502ac47`

while the latest #188 runtime receipt identifies:

`d07f7991579824b1b8b2087afae7c1b0ae5e6e7e`

`Kakashi gen87 QA fix: make presentation-owner diagnostic non-self-referential`

GitHub comparison shows the runtime head and current main are **diverged**:

- runtime head is **162 commits ahead** of current main;
- runtime head is **1 commit behind** current main;
- merge base is `0431dadc422b72ff00dd2a3af96cfc30fd588cd9`.

This must not remain ambiguous during completion.

### First execution requirement

Before further Kakashi implementation:

1. identify the exact active Coding branch / PR containing the latest #188 runtime head;
2. reconcile the current main-only coordination commit(s) into that active execution line;
3. establish one explicit **KAKASHI COMPLETION HEAD**;
4. do not alternate between stale main and a newer hidden runtime branch;
5. all subsequent source/integration proof must reference the exact completion head.

Do not throw away the 162-commit runtime lineage merely because main is behind.

Do not overwrite current main coordination authority merely because the runtime branch is ahead.

Reconcile them deliberately.

---

## 4. Authority freeze for the completion campaign

Coding must consume current authority, not historical issue-body shorthand.

At minimum:

- `Academy_Kakashi_Origin_100_Percent_Writing_Closure_2026-09-20.md`
- `Academy_Kakashi_100_Percent_Writing_Path_Variation_Audit_2026-09-20.md`
- `Academy_Kakashi_Origin_Completion_Gate_2026-09-17.md`
- #188 latest comments;
- #219 anti-stall + scope-locality rules;
- current CE custody / field-secured / group-transfer authority;
- current reward/development authority;
- current approved-dialogue verbatim rule;
- current participant-autonomy / Knowledge-gating rules;
- current Story Scene Board authority.

Historical superseded Kakashi docs remain archaeology only where current successor notices say so.

Coding may disagree with an authority if it finds a genuine contradiction or implementation impossibility, but must identify the exact conflict rather than silently rewriting Story semantics.

---

## 5. Build one finite Kakashi route manifest before changing more behavior

Coding must create or update one machine-addressable **Kakashi End-to-End Route Manifest** derived from current Writing closure.

The manifest is not new Story authority.

It is an implementation/QA index.

It must enumerate at least these root families:

1. **WATCH THE EXCHANGE**
   - STOP THE ASSASSIN
   - SECURE THE PACKAGE
   - SECURE THE PACKAGE BEFORE THE ASSASSIN
   - DEFEAT THE ASSASSIN, THEN SECURE THE PACKAGE
   - GO AFTER THE ORIGINAL TARGET

2. **MOVE IN CLOSER**
   - SUCCESS
     - LET THE HANDOFF HAPPEN
     - STRIKE BEFORE THE HANDOFF
     - ATTEMPT THE PICKPOCKET
   - FAILURE
     - STAY ON THE PACKAGE
     - STOP PACKAGE SMUGGLER
     - CUT THEM OFF AT THE SAKURA TREE

3. **Direct STRIKE BEFORE THE HANDOFF**

4. **Direct SLIP IN FOR THE PACKAGE**

For each route, the manifest must identify reachable factual variations relevant to implementation:

- resolver success/failure;
- pursuit success/failure;
- Battle victory/defeat;
- package recovered/lost/held elsewhere;
- MI/PS/AMT exact state;
- kill / restraint / ANBU / Police / release where authored;
- field-secured collection;
- group transfer;
- Pakkun present/absent/departed where legal;
- relevant Knowledge variants;
- terminal ANBU report;
- Minato private evaluation;
- Chronicle Receipt;
- reward/development;
- Origin completion;
- YOUR CHRONICLE BEGINS;
- exactly two teammates;
- first-Konoha boundary;
- Academy free play.

Do not hand-maintain dozens of disconnected "known fixed paths" in comments.

One manifest should define the finite implementation target.

---

## 6. End-to-end wiring law

Every manifest route must have one continuous executable chain:

```text
Origin selection
-> approved prologue
-> Scene Board
-> player intent
-> exact resolver / deterministic Story occurrence
-> factual commit
-> Battle when authorised
-> Battle result / claim
-> same Story return
-> participant + package state commit
-> exact continuation
-> later pursuit / Battle / disposition as applicable
-> ANBU report
-> Minato private evaluation
-> reward/development commit
-> read-only Chronicle Receipt
-> YOUR CHRONICLE BEGINS
-> exact-two teammate setup
-> first-Konoha boundary
-> Academy free play
```

No route is "implemented" if it stops at:

- a grey button;
- a dead-end choice;
- wrong Battle caller;
- black screen;
- generic fallback;
- missing return token;
- stale report gate;
- unclaimed reward;
- placeholder scene;
- duplicated report;
- missing Receipt;
- broken first-Konoha transition.

---

## 7. Canonical-owner consolidation is part of the same campaign

Coding must not finish Kakashi by stacking a new gen88/gen89/gen90 overlay above known conflicting owners.

For each Kakashi-critical responsibility identify one canonical live owner:

- choice materialisation / eligibility;
- factual resolver;
- Battle deployment;
- Battle return;
- package/participant commit;
- field restraint / collection / transfer;
- Pakkun continuation;
- Scene Board projection;
- transition/wipe;
- terminal debrief;
- Minato projection;
- rewards/development;
- Chronicle Receipt;
- Origin completion;
- loader/delivery.

When a stale owner is confirmed:

```text
map dependency
-> move any legitimate dependency to canonical owner
-> de-load stale owner
-> prove successor without it
-> delete/trim only when safe
```

The recent removal of duplicate MutationObserver / duplicate wipe rendering is the correct direction.

Continue that discipline.

Do not add another renderer/observer merely to beat an older renderer/observer.

---

## 8. Full-route automated traversal harness

The largest process improvement required by this campaign is a deterministic **Kakashi full-route traversal harness**.

Coding must not depend on Stephen manually replaying the entire Origin after each source change.

The harness should drive the actual current Story/runtime owners with controlled resolver/Battle outcomes and prove every manifest route reaches the correct next semantic state.

It must support deterministic selection of:

- Story choice;
- resolver success/failure where resolver-owned;
- Battle victory/defeat;
- relevant turn-window result;
- participant disposition;
- package outcome where resolver-owned;
- Pakkun factual branch where applicable.

For each route it must assert:

- expected next beat / situation;
- exact Battle config where applicable;
- same-Story return;
- package holder;
- MI/PS/AMT exact state;
- Pakkun state;
- reward receipt count;
- no duplicate occurrences;
- terminal state;
- Receipt projection source facts;
- completion state;
- no non-selected route facts.

The harness should produce a compact route matrix rather than hundreds of manual screenshots.

---

## 9. Save/load checkpoints inside the route harness

At minimum inject save/load or serialize/restore proof at high-risk boundaries:

- before first root choice;
- after resolver commit;
- before Battle;
- after Battle claim / before Story return;
- after field restraint;
- during multi-captive collection;
- before terminal report;
- after reward commit;
- at Chronicle Receipt;
- after Origin completion before first-Konoha.

Required invariant:

> **Reload reproduces the same committed Chronicle and does not reroll, duplicate, resurrect, recollect, retransfer or regrant anything.**

---

## 10. Presentation proof belongs in the completion campaign

The route harness alone is not Browser Golden, but it must catch deterministic presentation ownership problems where possible.

For every route family assert the expected:

- scene/backdrop ID;
- actor set;
- actor side/role placement where semantically fixed;
- dialogue/narration cue classification;
- no stale speaker labels;
- no duplicate report button where current authority says black wipe;
- no duplicate render owner;
- no unexpected raw escaped newline text;
- no missing Police/ANBU/Pakkun actors;
- no hidden participant leakage.

Approved dialogue must remain verbatim.

Presentation may not silently rewrite Story.

---

## 11. Debugging mode

During ONE-SWOOP MODE, every discovered defect belongs to the same campaign unless it proves an external-owner contradiction.

Coding should:

1. reproduce;
2. identify canonical owner;
3. fix;
4. add focused regression;
5. rerun affected route subset;
6. rerun full Kakashi route matrix at coherent checkpoints;
7. continue.

Do not stop after each defect to ask Stephen to retest.

Do not treat a generation bump as the deliverable.

Do not produce status-only responses while known editable Kakashi defects remain.

---

## 12. Scope-locality still applies

One-swoop does not mean "read the whole repository."

Use the route manifest to inspect only:

- the failing route owner;
- direct caller/loader;
- direct competing owner;
- exact QA.

Expand one dependency edge at a time.

The campaign is broad in **completion target**, not broad in speculative file inspection.

---

## 13. Installed-browser strategy

Coding cannot honestly claim installed-browser Golden from source/headless proof alone.

Therefore the target interaction with Stephen should be reduced to one consolidated acceptance loop.

### Before asking Stephen to test

Coding must have:

- reconciled completion head;
- full route manifest;
- every manifest route source/integration GREEN;
- stale owner cleanup complete enough that no known conflicting owner controls the path;
- loader/delivery parity proven;
- reward/idempotence proof;
- terminal/Receipt/completion proof;
- no known source defect remaining.

### Stephen's browser session

Stephen should then perform a compact representative installed-browser matrix, not another week of one-screen-at-a-time debugging.

Browser coverage must include enough representative routes to prove:

- each root family;
- win and defeat;
- package success and loss;
- lethal and nonlethal terminal;
- field restraint/group transfer;
- Pakkun route;
- Police route;
- ANBU route;
- terminal Hokage scene;
- reward claim;
- Receipt/completion/free-play.

If browser evidence finds a defect:

> **Kakashi remains RED and Coding resumes the same completion campaign immediately.**

Do not declare a new architecture phase.

Do not reopen Writing unless the defect proves an actual authority contradiction.

---

## 14. Stop conditions

Coding may stop the completion campaign only when one of these is true:

1. Kakashi is source/integration complete and Stephen's installed-browser acceptance is the only remaining gate;
2. one exact external-owner contradiction genuinely blocks a route and has been routed directly;
3. a hard tooling/context boundary is reached after committing/checkpointing recoverable progress;
4. Stephen must perform one exact local action that Coding cannot perform.

Invalid stop states:

- "I found the bug";
- "I think this is the cause";
- "CI is green";
- "gen88 is ready to test";
- "one branch is fixed";
- "I need another screenshot" when known source defects remain;
- "the module exists";
- "the guard is now wired";
- "browser should work now."

---

## 15. Completion definition

Kakashi is complete only when the existing binding completion gate is GREEN.

Final #188 evidence must include:

- exact completion HEAD;
- branch/main reconciliation;
- route manifest;
- all route-matrix results;
- canonical-owner map;
- retired/de-loaded stale owners;
- Battle/caller-return proof;
- package/participant-state proof;
- custody/group-transfer proof;
- Pakkun proof;
- terminal report + Minato proof;
- reward/development proof;
- Receipt/completion proof;
- save/load/idempotence proof;
- focused QA;
- broad #141 integration result;
- production loader parity;
- Stephen installed-browser approval.

Then, and only then:

`Kakashi Browser Golden: GREEN`

and Coding may propagate the clean shared Origin runtime to the other nine Origins under #105.

---

## 16. No new issue required

Do not create another competing Coding issue for this campaign.

Existing lane:

**#188**

Standing execution law:

**#219**

Final ten-Origin validation umbrella:

**#105**

This document changes the execution mode of #188 from repeated micro-repair to sustained end-to-end completion.

---

# Canonical lock

> **Academy Kakashi is now a finite Coding completion campaign. Consume the 100% closed Writing authority, reconcile the active runtime branch with main, build one route manifest, wire every route from Origin selection through free play, consolidate stale ownership instead of patch-stacking, prove the entire route matrix automatically, and only then hand Stephen one consolidated installed-browser acceptance pass. Browser RED returns to the same campaign until Kakashi is genuinely Golden.**
