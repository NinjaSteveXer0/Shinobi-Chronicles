# Shinobi Chronicles — Character Creation / Visual Legacy Audit Reconciliation

**Date:** 2026-09-09  
**Owner:** CE / CODEX / COORDINATION  
**Status:** **LEGACY AUDIT CONSUMED — LIVE AUTHORITY RECONCILED / OLD WORKSPACE MAY ARCHIVE**

## Purpose

This receipt consumes the final Character Creation / Visuals archaeological audit that reported `ARCHIVE READINESS: NOT SAFE` with eight proposed closures.

The legacy audit was valuable archaeological evidence, but its own evidence boundary stated that the live Chronicle Engine Design Bible and executable/current specialist repositories could not be fully inspected from that workspace. This receipt therefore reconciles those findings against current live GitHub authority before allowing them to become new blockers.

Production authority remains:

`current GitHub source/authority > durable specialist documents > current decisions > project memory > legacy chat archaeology`.

A local historical asset count must not overwrite a later live Registry gate.

---

# Executive verdict

The old visual workspace's **97 premium PNG identity set is archaeological asset evidence, not the current production Registry gate**.

Current production authority is the final **98 Characters + 18 Entities = 116** scope, with exact Registry-to-asset projection authority and a live 116-wide `uiPortrait` manifest. The 97 count must therefore remain genealogy only and must not be frozen as a new canonical Registry manifest.

The old apparent `1024×1024` versus `512×512` portrait contradiction is also superseded by current production authority: Alpha `uiPortrait` assets are **1024×1024**.

The remaining physical portrait work is already owned by GitHub issues #45 -> #16 and must not be duplicated.

The relationship/composite-identity finding is already represented in the live Chronicle Engine Design Bible's `Composite Identity Framework`; it is reinforcement, not a missing CE system.

No new SEND NOW handoff is created by this reconciliation.

---

# Eight legacy closure items — current disposition

## 1. Freeze a canonical 97-identity Registry-to-asset manifest

**Legacy finding:** local visual evidence contained 97 active premium PNG identities after Divided Vessel removal.

**Current disposition:** **SUPERSEDED AS PRODUCTION COUNT / RECORD ONLY AS ASSET GENEALOGY.**

Current Registry/Assets authority has advanced beyond that snapshot:

- final production destination/admission: **98 Characters + 18 Entities = 116**;
- `Documentation/Registry/Final 116 Admission Audit and Coding Handoff.md` authorises the atomic 116 gate;
- `Documentation/Assets/Final 116 Asset Projection Status.md` and `Documentation/Registry/Final 116 Asset Projection Registry Ratification.md` close the final projection wave;
- current portrait authority is 116-wide.

Do **not** create a new canonical 97 Registry manifest. If the old 97-card package is retained, it is an archaeological asset package with source/checksum value only.

Canonical rule preserved:

> **Registry identity != asset filename != folder position != historical ZIP membership.**

---

## 2. Resolve 1024×1024 versus 512×512 portrait contract

**Current disposition:** **CLOSED BY CURRENT LIVE AUTHORITY — 1024×1024.**

Current checked-in portrait QA and GitHub issues #45/#16 require every production `uiPortrait` to decode as PNG and be exactly **1024×1024**.

The historical 512×512 preference is superseded as Alpha production authority. It may describe an earlier optimisation idea or future derivative size, but it is not permission to replace the current 1024 masters.

Preserve:

> **source/master portrait contract != optional future derived runtime optimisation.**

Any future 512 derivative proposal requires a separate explicit migration/consumer contract and must not destroy the 1024 master authority.

---

## 3. Re-audit portrait coverage against 97 identities, especially Summons

**Current disposition:** **SUPERSEDED BY LIVE 116-WIDE QA — ALREADY ROUTED.**

Current issue #45 owns physical binary repair against the exact ratified **116-row Registry ID -> `uiPortrait` manifest**.

Its current repair lineage preserves exact mappings and explicitly includes high-risk Summon/Hosted/forced-manifestation rows. No 97-row remap should be created.

At the latest recorded #45 receipt in this reconciliation lineage, safe historical recovery had reduced the physical failures from 40 to 24 while keeping manifest/resolver/no-card-fallback checks GREEN. #45 remains the owner until its exact 116/116 physical gate is GREEN; #16 owns the downstream browser/runtime resolver proof.

No duplicate portrait issue is justified.

---

## 4. Confirm Coding consumes reconciled manifest rather than historical ZIPs/filenames

**Current disposition:** **CLOSED FOR MAPPING / RUNTIME-BROWSER PROOF REMAINS EXISTING TRAFFIC.**

Current #45/#16 authority explicitly states:

- runtime resolver is manifest-backed;
- no collectible-card fallback;
- no folder/filename inference;
- wrong dimensions or missing binary fail visibly rather than authorising a remap.

This closes the old visual-workspace uncertainty about whether Coding should consume historical ZIPs.

Browser/runtime final proof remains owned by #16 after #45 physical QA closes; that is implementation validation, not a new mapping/design question.

---

## 5. Coercive Cloak: Cards -> Combat -> PL -> Coding, or formally defer

**Current disposition:** **ALPHA SCOPE NARROWED; NO NEW FULL-ROUTE BLOCKER.**

Current source proves `coercive_cloak`, `three_tail_dominion`, `six_tail_dominion` and `breakout_kurama` are already stable live representation identities/assets under current Registry/Assets authority.

However, presentation approval still does not author mechanics. Therefore this reconciliation does **not** infer a complete coercive-route transition/acquisition/control chain from those assets.

For Alpha:

- retain every currently durable individual Registry/PL/Combat package exactly where already closed;
- do not create a new "complete coercive route" requirement merely because the card series exists;
- unresolved wider-route transitions, acquisition sequencing, forced-breakout integration or additional package IDs are **POST-ALPHA / caller-triggered future work unless a current Alpha Story/runtime caller explicitly requires them**;
- the generic mid-Battle Breakout Kurama forced-manifestation bridge is already explicitly deferred post-Alpha by current Combat closure;
- existing live identities/assets remain valid and are not deleted by this scope decision.

Thus the legacy `Cards -> Combat -> PL -> Coding` boundary remains an important ownership example, but it is not allowed to become a speculative Alpha blocker.

---

## 6. Shadow of Indra Stage-2 transformation/combat semantics if playable in Alpha

**Current disposition:** **NO NEW ALPHA STAGE-TRANSITION BLOCKER.**

Current Combat authority already closes Alpha-production semantics for `shadow_of_indra_black_mirror`.

Current forward Story planning treats Fallen Hokage Sasuke / Shadow of Indra as future route/end-state opportunity rather than a guaranteed Arc-1 encounter. The existence of the card does not establish that the full Fallen Hokage -> Shadow of Indra -> Sixth Shadow stage sequence is currently callable in the first Alpha Story run.

Therefore:

- preserve canonical progression naming: `fallen_hokage_sasuke -> shadow_of_indra -> sixth_shadow`;
- `Perfect Susanoo` remains superseded as a stage name;
- retain already-closed individual Combat semantics such as Black Mirror;
- defer the exact full Stage-1 -> Stage-2 -> Stage-3 transition/caller package until Story actually makes that staged boss progression executable.

No current Arc-1 Alpha blocker is created from unused future boss-stage choreography.

---

## 7. Soldier Pill, Antidote and Medical Kit assets + Inventory/Combat mappings

**Current disposition:** **LEGACY MOCKUP NAMES ARE NOT ALPHA ITEM AUTHORITY; DEFER UNTIL ITEM OWNER CALLS THEM.**

Current UI/runtime doctrine already closes the Battle Pouch as a live presentation over Inventory/Combat authority: one team-wide pouch, up to three prepared stacks/types, authoritative quantities/validity/reasons, no consumption on invalid selection, and successful use consuming the current actor's normal action.

Current repository search did **not** establish stable Alpha Item authority for the exact legacy labels `Soldier Pill`, `Antidote`, or `Medical Kit`.

Therefore the old visual request must not create these Items merely because they appeared in a mockup.

For Alpha:

- Battle Pouch may display only Items supplied by current Inventory/Items authority;
- no baked mockup label creates inventory quantity, targeting, effect, consumption or save/load truth;
- if Items/Combat later promotes these exact three named Items into Alpha, that owner must supply stable IDs/effects/consumption semantics and Assets/UI may then supply the corresponding presentation;
- until that occurs, this is **not an Alpha blocker**.

---

## 8. Relationship-owned collectible + deployment-footprint doctrines

### Relationship-owned / Shared-History collectible recognition

**Current disposition:** **ALREADY IN LIVE CE BIBLE — REINFORCEMENT / NO NEW CE SYSTEM.**

The live `Architecture/Composite Identity Framework.md` already establishes that:

- meaningful Shared History may ground a recognised pair/group Composite Identity;
- ordinary party/team co-presence does not automatically create one;
- runtime combination or participant ownership alone does not create persistent composite identity;
- participant ownership/access does not imply composite ownership/access;
- composite recruitment/collection/deployment must be explicitly authored.

This directly covers the reusable core of relationship-earned Duo/Group identities. SC-specific names such as `Team Seven United`, `Unbroken Bond` or `Final Rivals` remain future content, not new CE architecture.

### Multi-character deployment footprint / participant reservation

**Current disposition:** **SC-SPECIFIC QUEUE / NOT AN ALPHA BLOCKER UNTIL A CONSUMER EXISTS.**

Preserve the recovered non-collapse:

> **collectible selection identity != ontological participant set != deployment capacity footprint.**

And:

> **representation multiplicity != ontological duplication.**

Where a future multi-character collectible is deployable, its contract must expose the underlying participant references and an authored deployment footprint. Team/deployment validation must reserve embodied participant identities so the same persistent person cannot simultaneously appear through an individual and composite representation unless an explicit duplication mechanic permits it.

Deployment footprint is capacity cost only and must not modify PL.

This is queued for the future team/composite consumer; no speculative Alpha implementation is required today.

---

# Additional legacy findings preserved

The following useful archaeology remains valid and is retained as genealogy/consumer guidance:

- `breakout_kurama` remains a Summon-class Entity with `manifestationMode: forced_breakout`; invocation/manifestation mode does not redefine ontology;
- `Divided Vessel` is removed and must not return to active counts;
- Fallen Hokage Sasuke -> Shadow of Indra -> The Sixth Shadow is the canonical Sasuke boss genealogy;
- visual approval does not author mechanics;
- superseded/rejected asset representations may remain historically addressable without becoming active production content;
- local asset validation is not runtime/Golden validation.

---

# Archive readiness decision

## **OLD CHARACTER CREATION / VISUAL WORKSPACE: SAFE TO ARCHIVE AFTER THIS RECEIPT**

Reason:

- the dangerous 97-as-current-count interpretation has been explicitly superseded by live 116 authority;
- the portrait-size conflict is resolved at 1024×1024;
- outstanding portrait binary/runtime proof is already durably owned by #45 -> #16;
- Coding manifest discipline is already explicit;
- coercive-route and Shadow-of-Indra unused future transitions are formally prevented from becoming speculative Alpha blockers;
- the three legacy Battle Pouch item labels are not promoted to Item authority by presentation archaeology;
- the relationship/composite doctrine is already live in the CE Bible;
- future multi-character footprint enforcement is recorded as consumer-triggered SC queue work rather than a reason to keep the old chat alive.

Archiving this workspace does **not** mean the wider Alpha is complete. Existing #45/#16, Coding/browser work, Arc runtime integration and Golden/regression remain separate production work under their current owners.

---

# Routing

**No new SEND NOW dependency created.**

Existing active traffic is not duplicated.

- Portrait physical QA: #45 -> #16.
- Arc-1 first-run coordination: existing #36 chain.
- Relationship/composite identity: current CE Bible already authoritative.
- Coercive wider route, future staged Shadow-of-Indra boss transition, and generic multi-character footprint implementation: deferred/consumer-triggered, not Alpha blockers by mere asset existence.
