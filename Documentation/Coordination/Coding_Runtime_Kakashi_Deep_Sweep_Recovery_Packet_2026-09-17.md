# Shinobi Chronicles — Coding / Runtime Kakashi Deep-Sweep Recovery Packet

**Date:** 2026-09-17  
**Owner:** CE / Codex / Coordination  
**Status:** **BINDING SUCCESSOR / RECOVERY CHECKPOINT FOR ACTIVE KAKASHI #188**  
**Primary priority:** **FINISH SHINOBI CHRONICLES ALPHA**

## 1. Purpose

This packet makes the current Coding / Runtime deep-sweep recovery state durable so the active Coding workspace can be retired or replaced without losing production-critical continuity.

It does **not** redesign Story, Combat, Origins or Chronicle Engine. It records the current live ownership problem, current validation state and exact continuation order.

Standing Coding authority remains issue **#219** and its mandatory execution / consolidation / anti-stall charter. The active implementation lane remains **#188**.

Canonical precedence remains:

`GitHub live source > durable CE/SC documents > current specialist decisions > Project/chat memory`.

Always preserve:

`design closed != implemented != source/headless validated != production released != installed-browser validated != Golden`.

---

## 2. Source checkpoint

Pre-packet live `main` was:

`a02dd065c253848f5aa78c81b2ee6edcfe450a99`

`Add Objective to Kakashi scene authoring lock`

Its parent `4679eea46c8949fc7591f5d644325e01c6571dee` is also documentation-only.

Latest runtime-bearing checkpoint in this chain is therefore:

`e6e637348c42c832025b891937102ac2863a34a2`

`test: wire Kakashi Scene 2 direct-choice QA`

The source/headless Scene-2 direct-choice tranche is therefore durable. Installed-browser proof remains a separate level.

---

## 3. Current installed-browser evidence that must survive chat retirement

Stephen has current installed-browser evidence that **Scene 2 is correctly unlocked and all four initial Kakashi choices render active**.

Treat this as a durable validation checkpoint recorded by this packet, with these limits:

- it proves the current four-choice surface can reach Stephen's installed browser;
- it does **not** prove all four resulting routes;
- it does **not** prove ordinary non-hard-refresh delivery parity;
- it does **not** prove Kakashi Golden.

Current-generation browser traversal remains open for direct Attack, direct Pickpocket success, direct Pickpocket detected failure, Scene 3A, terminal scenes/rewards/Receipt and full save/load/idempotence.

---

## 4. Canonical owner / load classification

The current Kakashi runtime is **not yet a clean one-owner path**.

| Owner | Current classification |
|---|---|
| `32900` neutral Origin core / dispatcher | CANONICAL base Origin machinery |
| `runtime/alpha-origin-scenes-32900-c.js` Obito section | current production content |
| `runtime/alpha-origin-scenes-32900-c.js` Kakashi section | **SUPERSEDED-LIVE** |
| `runtime/alpha-kakashi-original-origin-restoration-33800.js` | **TRANSITIONAL / SUPERSEDED SEMANTIC SUBSTRATE** |
| `33900` Scene Board | CANONICAL generic presentation machinery |
| `33910` Kakashi Scene Board | current Kakashi presentation consumer |
| `34000` neutral Story Decision Realisation | CANONICAL semantic core |
| `34100` Kakashi final adapter/assembly | current Kakashi semantic assembly |
| `34200` stale-authority guard | current guard |
| `34300` Kakashi Battle deployment/caller | current Battle caller owner |
| `34410` sequential consumer | current sequential branch owner |
| `34500` Battle interaction / return bridge | current supporting owner |
| `34600` factual resolver provider | CANONICAL factual resolver seam |
| `34120` / `34700` | current factual commit / binding owners |
| `34710` | current direct Scene-2 Attack/Pickpocket consumer |
| `34800` | current Kakashi reward adapter |
| `34900` | current Substitution consumer |
| `35100` | current terminal debrief / reward-delivery owner |
| `35300` | current Pakkun interception consumer; downstream disposition still fail-closed |
| `35600` / `35610` | current sequential recovery / terminal Scene Board chain |
| `35700` | current Scene 3A consumer |

Distinct responsibilities may legitimately have distinct owners. The defect is that **old full Kakashi Story authorities remain loaded beneath current owners**.

---

## 5. Confirmed stale live Kakashi registrations

### 5.1 `32900-c` Kakashi is superseded-live

Current `runtime/alpha-origin-scenes-32900-c.js` still registers a complete obsolete Kakashi Origin using:

- Academy evaluator;
- logistics clerk;
- information broker;
- decoy-assassin pressure;
- obsolete first-choice and retrieval assumptions.

This is not current Kakashi Story authority.

Do not delete the whole file because its Obito section is current production content. Coding must surgically de-load/remove the **Kakashi registration responsibility** while preserving legitimate sibling content and any still-required neutral 32900 machinery.

### 5.2 `33800` remains a stale full semantic graph

Current `runtime/alpha-kakashi-original-origin-restoration-33800.js` unregisters and re-registers a full Kakashi graph and still contains stale expression/causal content including:

- `Hatake Kakashi.`;
- collapsed envelope handling;
- older Hokage-assignment wording;
- all four opening choices routing into a generic transfer beat;
- the transfer beat unconditionally stating that the package reaches the second man and a decoy assassin appears;
- the older major-choice surface.

Later layers correct/intercept this substrate, but that means stale authority can resurface when an interceptor is absent, stale, cached incorrectly or not applicable to one branch.

**Target:** current Kakashi semantics must no longer depend on obsolete full-graph authority remaining live underneath newer consumers.

Use the #219 rule: map direct dependencies -> migrate legitimate responsibilities -> de-load stale semantic responsibility -> focused proof -> then delete/trim obsolete code where safe.

Do not replace it with another overlay.

---

## 6. Confirmed production delivery mismatch

Current `runtime/alpha-story-decision-realisation-34000.js` requests:

`runtime/alpha-kakashi-final-origin-adapter-34100.js?sc=kakashi-final-20260917-20`

Current `runtime/alpha-kakashi-final-origin-adapter-34100.js` declares:

`const BUILD="kakashi-final-20260917-22";`

and Generation 22 includes the current Scene-2 direct Attack/Pickpocket consumer.

Therefore parent delivery and child delivery are not on one coherent generation.

This does not prove every hard-refresh load is stale, but it means ordinary production delivery parity is **not closed**.

### Immediate Coding correction

Fix the existing canonical parent/child delivery contract. Do not create a compatibility module merely to hide the mismatch.

Then prove with an ordinary installed-browser reload that Scene 2 still presents the current four-choice surface.

---

## 7. Issue reconciliation

### #188

**OPEN / ACTIVE / primary Kakashi implementation lane.**

Do not close until the binding Kakashi Completion Gate is installed-browser GREEN and Stephen approves.

### #192

**OPEN / ACTIVE, but issue body is partly stale.**

The old `alleyway_konoha_night` missing-path blocker is no longer the live blocker; the asset exists/binds in the current Kakashi presentation chain.

Current `35300` diagnostics explicitly prove:

`victoriesOpenExistingDisposition`

while also requiring:

`dispositionExecutionFailClosed`

with all choices on the disposition-pending beat unavailable.

Therefore the real surviving #192 implementation gap is the **post-victory `AK_SA_013` disposition execution path**, plus installed-browser proof of the current branch chain.

### #201

**OPEN / state reason reopened.**

Current inspected evidence supports treating the ten-config Combat consumption as already implemented/consumed. No newer concrete implementation delta was identified by this recovery pass that justifies making #201 the active next Coding task.

Classification for successor priority:

**IMPLEMENTATION APPEARS CONSUMED / OPEN-REOPENED LEDGER DRIFT unless a newer concrete reopen reason is identified from current evidence.**

Do not close #201 from this packet alone; do not prioritize it merely because it is OPEN.

### #209

**CLOSED / completed. RECORD ONLY.**

Do not re-enter first-Konoha tutorial implementation merely because an older audit called it next.

### #219

**OPEN standing Coding protocol.**

Mandatory for every current/future Coding workspace.

---

## 8. Current validation state

| Layer | Current defensible verdict |
|---|---|
| Writing / semantic authority for closed Kakashi slices | GREEN |
| Scene-2 four-choice implementation/source QA | GREEN |
| Current installed-browser initial four-choice surface | GREEN — Stephen evidence, now durably recorded here |
| Historical installed-browser Battle launch/input/defeat-return/victory | GREEN on earlier current-generation paths |
| Current-generation direct Attack browser result | OPEN |
| Current-generation direct Pickpocket success | OPEN |
| Current-generation direct Pickpocket detected failure 3-v-1 | OPEN |
| Current-generation Scene 3A | OPEN browser proof |
| Current-generation terminal ANBU report | OPEN browser proof |
| Current-generation reward/development delivery | OPEN browser proof |
| Current-generation Pakkun departure | OPEN browser proof |
| Current-generation Minato evaluation/cutaway | OPEN browser proof |
| Chronicle Receipt / `YOUR CHRONICLE BEGINS` | OPEN browser proof |
| full save/load/reopen/idempotence | OPEN current-generation browser proof |
| Kakashi Golden | **RED / NOT CLAIMED** |
| #105 ten-Origin Golden | **RED / NOT CLAIMED** |
| whole Alpha Golden | **RED / NOT CLAIMED** |

Do not regress to the obsolete statement `Kakashi Battle has never worked`. Battle has historical installed-browser proof. The current task is latest-generation end-to-end proof plus ownership consolidation.

---

## 9. Exact continuation order

Coding should now work from the smallest justified causal scope under #219:

1. **Correct `34000 -> 34100` delivery parity.** Parent must request the intended current generation. Add/adjust the narrow delivery regression. No new compatibility module.
2. **Ordinary browser reload replay.** Confirm Scene 2 still shows all four opening choices active without relying on Ctrl+F5.
3. **Run `STRIKE BEFORE THE HANDOFF`.** Prove current semantic Attack execution and ensure stale `33800` generic-transfer truth does not leak into the route.
4. **Run `SLIP IN FOR THE PACKAGE` through both factual outcomes.** Success = authorised Kakashi package custody with no invented MI/Pakkun. Detected failure = authorised MI-visible exception + exact 3-v-1. Reopen/save-load must not reroll.
5. **Run `WATCH THE EXCHANGE` into current Scene 3A** and prove current Scene-3A presentation / choice state in the installed browser.
6. **Consolidate live Kakashi ownership.** Map the direct dependencies and surgically de-load the obsolete Kakashi registration in `32900-c`, then retire the stale semantic responsibilities still living in `33800`. Preserve legitimate asset/presentation responsibilities until successor ownership is explicit. Do not mass-delete first.
7. **Continue approved scene/branch work through #192's `AK_SA_013` disposition family** until the currently fail-closed disposition choices have real authorised execution owners.
8. **Run the complete latest-generation terminal path in the installed browser:** same-Story Battle return, participant classification, package/custody state, Pakkun departure where due, terminal ANBU report, reward/development delivery, Minato evaluation/cutaway, Chronicle Receipt, `YOUR CHRONICLE BEGINS`, first-Konoha orientation, Academy continuation, save/load/idempotence.
9. **Only after Stephen approves the complete installed-browser result may Kakashi/#188 become Golden.**
10. **Then propagate the proven shared Origin runtime and execute #105 across the other nine Origins.**

Do not start a broad ten-Origin rewrite while Kakashi is still layered and RED.

---

## 10. Scope-locality reminder

The deep sweep is finished. Do not continue broad repository archaeology as normal Coding behavior.

For each step above:

`known owner -> direct caller/loader -> direct competing owner -> subsystem path only if evidence requires it`.

Example:

- generation parity: inspect `34000`, `34100`, and the exact delivery QA—not unrelated World/Combat/UI files;
- stale 32900-c Kakashi: inspect its Kakashi registration, registration caller/loader, current successor ownership and affected QA—not every Origin file;
- `AK_SA_013`: inspect `35300`, the exact authority/consumer and focused branch QA—not the entire Story stack.

Long execution session means stay on the active causal problem until material progress lands. It does **not** mean scan the repository indiscriminately.

---

## 11. Archive-safety declaration

This packet exists specifically to externalize the current Coding-chat-only recovery state.

Once this commit is present on GitHub and linked from active #188, the current Coding / Runtime chat is **archive-safe from a continuity perspective**.

That statement does **not** mean:

- Kakashi is complete;
- #188 is complete;
- installed-browser Golden is GREEN;
- the stale code is already retired.

It means a successor Coding workspace can recover the exact current production state, current owner classification, known delivery mismatch, issue reclassification, browser checkpoint and continuation order without needing the retiring chat.

---

## 12. Non-collapse reminders

Preserve throughout:

- Battle victory != Story success / custody / death / reward entitlement;
- defeated != dead / captured / controlled;
- custody != ownership / Inventory;
- presentation != semantic authority;
- Story choice label != resolver result;
- source/headless GREEN != browser Golden;
- cache bust != semantic repair;
- new correct code existing != production consuming it;
- stale code being old != safe to delete;
- Origin != Mission;
- no second Story/history system.

**Stephen relay:** NONE. GitHub is the durable recovery authority.