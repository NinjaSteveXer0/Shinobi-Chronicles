# Academy Mirai — WRITING GOLDEN / Disguised-Instructor Implementation Reconciliation

**Date:** 2026-09-24  
**Owner:** CE / Codex / Coordination  
**Status:** **BINDING CROSS-SYSTEM RECONCILIATION — DOWNSTREAM IMPLEMENTATION PENDING**  
**Production Origin:** `academy_mirai`  
**Primary priority:** **FINISH SHINOBI CHRONICLES ALPHA**

## 1. Purpose

This reconciliation consumes Academy Mirai WRITING GOLDEN and closes the downstream ownership/order needed to implement it without reopening Story.

Writing authority:

`Documentation/Story/Academy_Mirai_Origin_WRITING_GOLDEN_2026-09-24.md`

Writing authority commit:

`e766944f04c82a30fcde84646a00c89814bf4f78`

AMBER candidate supersession commit:

`3d050be863d4f215bdffa13051ca13baf222134a`

## 2. Story truth now closed

The optional Mirai PL Battle opponent is the **apparent Escort after the covered-market switch**.

World Truth:

> The Academy instructor switched places with the real Escort during the covered-market separation and is currently presenting as the Escort.

Preserve:

- real Escort != Academy instructor;
- real Escort is safe after the switch;
- apparent Escort after the switch = disguised Academy instructor;
- a PL Battle against the apparent Escort is therefore a PL Battle against that instructor;
- disguise presentation does not mint a second hidden combatant;
- World Truth does not automatically grant Mirai/player-character Knowledge;
- the authored reveal remains Story-owned;
- no unrelated enemy is authorised for this Origin.

This same identity truth applies to every currently approved Mirai branch that legitimately escalates into a post-switch PL Battle, including the shortcut/confrontation paths.

## 3. Existing occurrence facts remain separate

Preserve:

- MIR-01 `occ_origin_mirai_substitution_verification_resolution`;
- MIR-02 `occ_origin_mirai_changed_chakra_observation`;
- MIR-03 `occ_origin_mirai_checkpoint_escort_resolution`.

Do not collapse:

```text
physical escort success
!= changed-chakra observation
!= identity verification
!= Battle result
!= final Mirai reflection
```

## 4. Current source audit result

Current live authority was refreshed before routing.

No current stable Registry identity / PL calibration for the unnamed Mirai Academy instructor was found in the current production Registry/PL authority or source search.

Therefore CE will **not** invent:

- a canon identity;
- a PL number;
- Stats;
- techniques;
- Rank;
- hidden alternate representation.

That closure belongs to PL / Registry / Rank first.

## 5. Required dependency order

The smallest safe implementation chain is:

```text
1. PL / Registry / Rank
   -> stable instructor identity / representation
   -> Base Stats / Base PL / Current PL boundary

2. Combat / Skills / Items / Weapons
   -> executable Battle package
   -> exact legal Skills/actions
   -> Battle win/loss return contract

3. Coding / Runtime
   -> replace current compressed Mirai runtime expression
   -> bind exact GOLDEN content
   -> call exact Battle package
   -> project exact post-Battle return
   -> prove save/load / idempotence / installed-browser routes
```

Do not start Coding by inventing temporary Battle values or a placeholder opponent package that later becomes production authority.

Do not make Stephen relay these dependencies manually.

## 6. PL / Registry scope

PL / Registry / Rank must publish a stable production identity suitable for the unnamed Academy instructor.

The representation may remain role-based / unnamed.

Do not invent a famous canon character merely to fill the role.

PL / Registry owns:

- stable entity/representation ref;
- entity type / representation classification;
- Stats;
- Base PL;
- Current PL boundary for this Battle;
- formal Rank only if authority genuinely requires/knows it.

The Battle should be appropriate to an Academy assessment context, but CE does not infer the numeric calibration.

Disguise does not create a second identity.

Conceptually:

```text
one Registry participant
+ disguised presentation as Escort
= one combatant
```

## 7. Combat scope

Combat consumes the PL/Registry result.

Combat owns:

- exact Battle config/package;
- legal Skill/action kit;
- action economy;
- target legality;
- hit/damage/state behavior;
- win/loss Battle result;
- exact Battle return result envelope.

Combat must not rewrite the Mirai Story or decide the identity reveal.

The disguised presentation can remain “Escort” to the player until Story reveals the instructor, while Combat/runtime internally targets the one stable participant ref.

## 8. Coding scope

After Registry/PL and Combat close their dependencies, Coding must replace the current compressed Academy Mirai expression in:

`runtime/alpha-origin-scenes-32900-a.js`

with the exact WRITING GOLDEN content.

This is a **REPLACE**, not additive patch, for the superseded Mirai player-facing expression.

Coding must preserve:

- exact approved narration/dialogue/choice wording;
- covered-market switch;
- route-dependent Knowledge/evidence availability;
- shortcut/confrontation Battle call;
- exact post-Battle Story return;
- checkpoint reveal variants;
- final reflections;
- Origin completion;
- MIR-01/MIR-02/MIR-03 source addressing;
- save/load/idempotence;
- shared Scene Board/choreography ownership;
- universal player-facing Writing quality gate.

Coding must not:

- paraphrase approved Story;
- add CE/QA explanatory prose;
- invent a replacement enemy;
- make Battle reveal hidden identity early;
- collapse evidence/verification/Battle/checkpoint facts;
- claim Browser Golden from source/headless GREEN.

## 9. Presentation boundary

Use current shared presentation ownership.

World Truth / Registry identity may be the Academy instructor while the player-facing Battle presentation still shows the apparent Escort if the current Story state has not revealed the disguise.

Preserve:

```text
World identity != observer Knowledge != presentation label
```

The Battle asset/presentation projection must not leak the hidden instructor identity before the authored reveal.

## 10. Validation minimum

Before Browser Golden:

- verify TALK / PROFESSIONAL early branches;
- verify market switch preserves real-Escort history;
- verify shortcut Battle;
- verify confrontation Battle where eligible;
- verify non-Battle verification route;
- verify no-verification checkpoint route;
- verify changed-chakra route;
- verify conversation-contradiction route;
- verify Battle win return;
- verify Battle loss return;
- verify reveal timing does not leak;
- verify MIR-01/MIR-02/MIR-03 independently;
- verify final reflection remains independent;
- verify save/load before and after the switch;
- verify save/load around Battle;
- verify no duplicate occurrence/reward/result commit;
- verify exact approved text remains intact;
- verify no stale compressed Mirai prose can regain production control.

## 11. Status

- Story design: **CLOSED**
- player-facing Writing: **GOLDEN**
- Battle opponent Story identity: **CLOSED**
- Registry identity/calibration: **OPEN**
- Combat package: **WAITING ON REGISTRY / PL**
- Coding runtime replacement: **WAITING ON REGISTRY / PL + COMBAT**
- installed-browser validation: **NOT YET**
- Browser Golden: **NOT CLAIMED**

## 12. Final lock

> **Academy Mirai's optional post-switch PL Battle is against the same Academy instructor who is currently disguised as the apparent Escort. Registry/PL must first establish the one stable combatant identity and calibration; Combat then defines the executable Battle package; Coding then replaces the superseded Mirai runtime with the exact WRITING GOLDEN content. Hidden World identity must remain distinct from Mirai Knowledge and player-facing presentation until the authored reveal.**
