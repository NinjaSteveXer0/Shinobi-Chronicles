# Shinobi Chronicles — Pairing / Bond Alpha Admission Asset Boundary

**Status:** ASSETS / UI SCOPE LOCK — OPTION A SUPPORTED; REGISTRY RATIFICATION + PRODUCTION ASSET PROJECTION REQUIRED BEFORE LIVE ACTIVATION  
**Date:** 2026-09-04

This document responds to the Post-899 Coding admission boundary for the five identities whose Alpha pairing/bond semantics are already closed.

## 1. Alpha scope decision

Assets/UI supports **Option A** for Alpha:

- `sj_kiba` — Character
- `sannin_hinata` — Character
- `sannin_sumire` — Character
- `akatsuki_kakuzu` — Character
- `nue` — Entity / Summon

The authorised target gate after Registry ratification, Coding ingestion, and asset projection closure is:

**89 Characters + 18 Entities = 107 live production identities**

The current runtime gate remains **85 Characters + 17 Entities = 102** until those downstream steps actually occur.

The remaining eight Characters in the separately calibrated awaiting-placement wave are **not part of this Alpha admission cohort** merely because calibration/card concepts exist.

Preserve:

**admission scope authorised ≠ live Registry ingestion**

**calibration authority ≠ production admission**

**production admission ≠ acquisition**

## 2. Stable identity/classification boundary

| Registry representation ID | Classification | Alpha admission scope |
|---|---|---|
| `sj_kiba` | Character | AUTHORISED COHORT |
| `sannin_hinata` | Character | AUTHORISED COHORT |
| `sannin_sumire` | Character | AUTHORISED COHORT |
| `akatsuki_kakuzu` | Character | AUTHORISED COHORT |
| `nue` | Entity / Summon | AUTHORISED COHORT |

`sj_kiba` is the only valid production Kiba representation ID for this cohort.

Do not create `s_jkiba` or another alias from display prose.

## 3. Current repository asset audit

The current live repository contains no standalone production collectible-card file or dedicated approved square `uiPortrait` file for any of these five identities in the existing production asset hierarchy.

The current 102-row Battle Portrait manifest and Registry ratification therefore remain valid for the existing 102 gate and must not be heuristically extended.

Current unresolved production projections:

| Registry ID | collectibleCard | uiPortrait | Assets activation status |
|---|---|---|---|
| `sj_kiba` | NOT YET PUBLISHED TO LIVE REPO | NOT YET APPROVED/PUBLISHED | NEEDS PRODUCTION ASSET PROJECTION |
| `sannin_hinata` | NOT YET PUBLISHED TO LIVE REPO | NOT YET APPROVED/PUBLISHED | NEEDS PRODUCTION ASSET PROJECTION |
| `sannin_sumire` | NOT YET PUBLISHED TO LIVE REPO | NOT YET APPROVED/PUBLISHED | NEEDS PRODUCTION ASSET PROJECTION |
| `akatsuki_kakuzu` | NOT YET PUBLISHED TO LIVE REPO | NOT YET APPROVED/PUBLISHED | NEEDS PRODUCTION ASSET PROJECTION |
| `nue` | NOT YET PUBLISHED TO LIVE REPO | NOT YET APPROVED/PUBLISHED | NEEDS PRODUCTION ASSET PROJECTION |

Composite gallery/reference art is not a valid replacement for a standalone production card or a dedicated square Battle/UI portrait.

Do not:

- invent repository paths from Registry IDs;
- crop collectible-card/composite artwork into permanent `uiPortrait` authority;
- infer approval from folder names or filename similarity;
- substitute another existing representation's portrait;
- mark one of these five ACTIVE before exact card/portrait paths are explicitly approved.

Preserve:

**Registry identity ≠ asset filename**

**asset existence ≠ approved runtime mapping**

**collectible Character Card ≠ `uiPortrait`**

**presentation asset identity ≠ production ontology**

## 4. Pairing/bond ontology remains unchanged

This asset/admission scope decision does not alter the already-closed pairing semantics:

- `sj_kiba` is one integrated Kiba + Akamaru collectible/deployment representation; Akamaru is causally addressable but not a second collectible slot;
- `sannin_sumire` and `nue` remain two stable Registry identities with paired acquisition/default attachment and independent Summon lifecycle;
- `akatsuki_kakuzu` mask sources remain embedded Alpha presentation/causal sources rather than autonomous participants;
- `sannin_hinata` remains the separately calibrated alternate-growth representation.

## 5. Required completion path

To turn the authorised 107 target into a live production gate:

1. Registry explicitly ratifies Option A and the five classifications/IDs above.
2. Assets publishes/approves one exact standalone collectible-card mapping where required for each identity.
3. Assets publishes/approves one exact square `uiPortrait` path for each identity.
4. Registry ratifies the five identity → `uiPortrait` projections.
5. Coding ingests the five records and extends the single portrait resolver from 102 → 107.
6. CI extends the portrait manifest/Golden gate and proves all 107 expected IDs resolve exactly once with valid approved assets.

Until steps 1–4 are complete, Coding must not fabricate the missing asset authority merely to satisfy the new count.

## 6. Full-wave boundary

The separately calibrated awaiting-placement wave contains twelve Characters plus `nue`, which would produce **97 Characters + 18 Entities = 115** if all thirteen were admitted.

That larger wave remains awaiting placement. This Alpha decision deliberately does **not** admit the other eight Characters.

A later full-wave admission requires an explicit new Registry/Assets production-admission decision rather than piggybacking on the five pairing/bond consumers.
